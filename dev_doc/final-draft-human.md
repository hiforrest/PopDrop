## 软件名称

<img src="小众论坛帖子v2_图床.assets/c7bf34bb5ddb1f8b1a6e32ebff861137a62d48b3_2_384x384.webp" width="192px">

PopDrop

## 官网

- [GitHub](https://s.ee/popdrop-github)
- [网盘下载](https://s.ee/popdrop-download)

## 推荐类型

【开发者自荐】

## 使用门槛

- 是否付费：否
- 是否需要联网：不需要

## 一句话简介

按一下 `F2`，文件面板出现在屏幕最前面。找到文件，拖走，关掉。全程不到三秒，文件还没反应过来，已经被你发送出去了。

PopDrop 是一款 Windows 文件快捷面板。它把常用文件夹、最近打开的文件和固定好的文件集中在一个面板里，需要时叫出来，用完了，挥之即去。

## 主要功能

### 快捷键呼出最近文件，拖拽到其他软件

![img001.avif](https://i.see.you/2026/07/23/atE4/img001.avif)

按 `F2` 呼出 PopDrop 窗口。多个目录里的文件会独立分栏，每栏的新文件、最近编辑过的文件，都排在前面。

看到需要的文件，直接拖进微信、飞书或其他软件。事情办完，面板自动退场。

### 任意拖入文件（2026-07-27 新增）

你也可以从其他文件夹、聊天软件、网页等位置，直接把文件拖进 PopDrop。

不必先复制，再打开目录，再粘贴。直接把文件拖拽到具体分栏，文件就进入对应文件夹，没必要到处办手续。

### 固定项

![img002.avif](https://i.see.you/2026/07/23/raJ7/img002.avif)

可以把任意文件拖进「固定项」栏，也支持拖入文件夹。固定之后，还能把这些文件拖到其他软件或窗口里。

所以，“固定项”可以当成一个文件架，也可以当成文件中转站。比如某几份资料这两天需要反复查看、修改或者发送，就先放在这里，伸手就能拿，不用随时放在。

需要说明的是，取消固定只会移除「固定项」里的记录，不会删除原文件。

### 多工作区（2026-07-27 新增）

每个工作区可以配置不同的文件夹组合，“固定项”也相互独立。

手上同时推进几个项目时，可以给每个项目分一套工作区，不必挤在一起。

### 右键菜单

- 可以根据扩展名绑定打开程序。
- 可以添加带运行参数的动作，实现更强大的功能。

## 为什么有用？

窗口最恼人的地方，主要有两个：

- 出现的时候挡住其他窗口，你不得不来回切换。
- 该消失的时候还待在原地，像会议已经结束了，还有人坐着不走。

### 不是全屏，只是一个小面板

面板小，就不容易挡住其他窗口。两个窗口可以同时出现，也就不必为了拿一个文件，在不同界面之间来回切换。

比如你正在微信或飞书里跟人沟通，突然需要发送一份近期的工作文件。

使用传统的 Total Commander 时，你要先切换到 Total Commander，找到文件，按住左键抓住它，再重新激活微信窗口，最后把文件拖进去。

这一套流程不难，但窗口实打实切换了两次。只是想发个文件，操作焦点也得跟着来回切换。

窗口切换容易打断工作记忆。注意力被迫转移后，如果你在新窗口停留得久一点，再切回原窗口，往往还得花一点时间想：我刚才说到哪了？

而使用 PopDrop，你不用离开微信。按一下 `F2`，工作文件就在手边，直接拖进微信。拖完继续聊天，PopDrop 会自动隐藏。

不用另开文件管理器，不用来回切换窗口，也没有一串为了完成简单任务而存在的复杂操作。

### 需要时呼之即来，该消失时自动隐藏

![img003-2.avif](小众论坛帖子v3.assets/img003-2.avif)

基于这个思路，PopDrop 的窗口被设计成“临时置顶”。

呼出时，它会出现在顶层，方便你直接操作。不需要时，点击或切换其他窗口，它就会自动隐藏。

不过，它也不是一碰就跑。

如果当前发生的是拖拽动作，比如你正从其他窗口把文件拖进 PopDrop，它不会自动隐藏。

### 快捷启动栏

![P_260723_204346.avif](https://i.see.you/2026/07/23/Mr9i/P_260723_204346.avif)

把常用软件的快捷方式放进一个文件夹，再在 PopDrop 配置中添加该文件夹，并将属性设置为 `Mode=Launcher`，这个文件夹就会变成快捷启动栏。

快捷启动栏默认按照文件名排序。

也可以在文件名前添加“010”“020”等序号前缀，自定义显示顺序。实际展示时，序号和扩展名都不会显示。

### 最近打开

点击“近期栏”按钮，可以显示 Windows 记录的近期打开文件。

## 使用方法

1. 下载并解压软件。
2. 双击运行 `PopDrop.exe`。
3. 按 `F2` 呼出面板。

没有账号，没有登录，直接开始。

## 适用人群

需要打开、编辑、管理、发送或接收各种文件的 Windows 用户。

尤其适合每天在聊天软件、办公软件和文件夹之间来回搬文件的人。桌面上开着微信，PopDrop 在你的手边食客准备着，需要哪份资料就拖哪份，发完继续聊，不必让文件管理器突然冲出来打断思路。

## 已知限制

仅支持 Windows。

“最近打开”功能依赖系统记录；在不同权限级别的软件之间拖放文件时，可能受到系统限制。

## 安全说明

PopDrop 不会主动修改、移动或删除文件。

软件使用 AutoHotkey 开发，因此可能被部分安全软件误报。遇到提示时，请根据软件来源和实际情况自行判断。

理想的使用场景大概就是：聊天窗口还开着，对方刚说“把那个文件发我一下”，按下 `F2`，拖过去，面板自己收起来。就像什么都没发生过。

让同时推进好几个项目的工作小能手，一切尽在掌握。