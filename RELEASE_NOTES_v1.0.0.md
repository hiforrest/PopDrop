# PopDrop v1.0.0 — 稳定版发布

经过从 v0.7 到 v0.10 的持续迭代，PopDrop 正式进入 1.0.0 稳定阶段。

## 主要变化

### 源码模块化重构

- 将原 12,996 行的 `PopDrop.ahk` 按功能拆分为 `modules/` 目录下的 **14 个独立模块**（配置、应用动作、面板 UI、扫描缓存、项目操作、右键菜单、指针输入、文件操作、OLE 投放、Shell 拖出、自测和生命周期）
- 入口文件只保留启动、共享状态、初始化和消息注册，不再承载业务逻辑
- 模块化使用 AHK v2 文本 `#Include`，不改函数签名、调用链和全局状态模型，**零行为变更**

### 工程整理

- 开发报告、测试报告、热修复笔记、本地构建配置等开发期文档统一移入 `dev_doc/` 目录（gitignored）
- 根目录仅保留发布相关的核心文档（README、CHANGELOG、USAGE、BUILD、THIRD_PARTY_NOTICES）
- 修复 `--self-test` 在模块化后的启动顺序问题，确保自测可靠性
- 更新 Ahk2Exe 解释器存根为 v2 版本，恢复 v2 脚本编译能力

### 版本号

- 从 `0.10.0` 升至 **`1.0.0`**，标记项目进入稳定阶段

---

## 从 v0.10.0 以来的完整功能回顾

### 文件内容预览（v0.10.0）

- 新增 Markdown、文本/日志/配置、代码和 CSV/TSV 的有界语义快照
- 新增 PDF 第一页和 DOCX 开头内容预览，PDF 使用 Windows 内置渲染
- 新增 Seer/QuickLook 空格键集成（默认关闭）
- 新增常驻 `PopDropPreview.exe` 原生 Helper，使用共享内存和事件通信
- 文件悬浮预览窗口，自动选择侧边，支持图片、文档、文本
- 后台缓存生成，面板隐藏 10 秒后低优先级生成快照
- 缓存键包含文件身份、大小、修改时间、尺寸桶和规格版本

### 文档预览增强（v0.10.0）

- PDF 首屏新增可选 PDFium 渲染路径（需用户同意下载）
- Markdown/文本绘制改为显式逐行切分再单行绘制，消除覆盖问题
- 设置页新增独立 PDF 预览开关和组件状态管理
- 外部预览窗口 Z 序管理，Seer/QuickLook 持续保持在 PopDrop 之上

### 文件管理器适配器（v0.9.0）

- 支持 Windows Shell、Directory Opus、Total Commander、XYplorer、Double Commander、FreeCommander XE、Files 共 7 个文件管理器
- 设置页提供文件管理器选择、路径自动查找和测试按钮

### 来源管理（v0.9.0）

- 主面板来源管理菜单：右键分组标题可打开文件夹、刷新、精准定位设置或安全移除
- 来源移除使用配置备份和原子事务
- 拖拽文件夹快速添加为来源，工具栏临时切换为添加/固定双区域
- 来源显示数量支持继承全局/自定义/全部三种模式

### 应用工具动作系统（v0.9.0）

- 同一条应用保留稳定记录，可配置多个带稳定 ID 的工具动作
- 支持文件、文件夹、混合选择、串行/批量执行模式
- 动作参数支持 `{folder}`、`{parent}`、`{date}`、`{time}`、`{size}` 等变量
- 右键菜单前 5 项直接显示，其余进入"更多工具操作…"

### 外部内容投放（v0.8.0）

- 浏览器图片拖放：支持 PNG、DIB/V5、URL 下载，自动去重和质量收敛
- 异步 HDROP 接管：helper 单次读取，消除浏览器延迟文件重复生成
- 多适配器格式分类器（CF_HDROP → 虚拟文件 → 图片 → URL）
- 原生 `PopDropTransfer.exe` 独立进程，COM 标准 marshaling

### 本地文件操作（v0.7.0–v0.8.0）

- 完整 OLE IDropTarget，支持 Files/Launcher/固定项投放
- 右键精简菜单，支持按扩展名配置打开程序
- 复制到…/移动到…（IFileOperation），支持常用/最近目标
- 回收站删除、重命名（IFileOperation）
- 固定项内部排序

### 工作区系统（v0.9.0）

- 多工作区管理，每个工作区独立保存来源和固定项
- 设置窗口左侧导航，支持新建、复制、重命名、删除
- 配置原子写入和失败回滚

### 设置界面（v0.7.0–v0.9.0）

- 四页原生 Tab3 设置窗口，独立内存草稿，统一校验
- 来源管理、打开软件、常用位置、排除规则图形化管理
- 配置版本升级链路，旧配置自动迁移

### 其他

- 噪声过滤（Hidden/System/Temporary 属性、通配忽略规则）
- 单击/双击打开文件选择
- 默认右键菜单选择（PopDrop/系统菜单）
- 快捷键统一分派，下载任务管理
- 下载即时呈现，大文件图片质量收敛
- 跨平台 Python 自测试（47+ 项）
- 配置文档引擎（ConfigDocument），解决 INI 写入节顺序错乱

---

## 下载

- **PopDrop.exe** — 主程序（x64）
- **PopDropTransfer.exe** — 外部内容投放 Helper（x86/x64）
- **PopDropPreview.exe** — 文件预览 Helper（x86/x64）
- **config.example.ini** — 配置示例

## 系统要求

- Windows 10 或 Windows 11
- AutoHotkey v2（仅源码运行需要）