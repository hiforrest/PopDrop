# 工作区热视图提速

纯内存扫描快照解决了配置与磁盘 I/O，但原先仍在每次 Tab 切换时清空同一个 Win32
`ListView`，逐项重建分组、行、图标和布局。文件数量增加后，这条同步 UI 路径仍明显慢于
浏览器标签页。

本次优化为每个访问过的工作区保留一套已经填充的原生主列表视图及其运行时元数据：

- 主 `ListView` HWND；
- 行到路径、类型、来源和打开上下文的映射；
- 分组与投放目标映射；
- ImageList、缩略图索引缓存和待增强队列；
- 文本块搜索索引、查询和待处理队列；
- 渲染状态、计数及工作区状态文案。

## 零固定项命中修复

Windows ListView 的分组编号并不表达业务类型：PopDrop 中固定项分组使用 group id 1，而没有
固定项时，第一个来源分组同样从 group id 1 开始。旧校验在固定项数量为零时仍检查第一行是否
属于 group id 1，于是把正常来源行误判为“缓存中多出固定项”，使热视图在最常见的工作区配置
上永远失效。

当前逻辑在固定项为零时不再用第一行的原生 group id 猜测固定项数量；工作区 ID、配置指纹、
固定项签名、扫描完整性、DPI、完整布局、搜索条件和保存对话框 Task Link 上下文等门禁仍全部
保留。固定项非零时仍逐行验证固定分组，并拒绝数量不一致的缓存。

再次访问工作区时，若工作区所有权、配置指纹、固定项内容、原生固定分组、当前 DPI 和完整布局签名均
匹配，并且“另存为”Task Link 上下文与当前 invocation 一致，切换路径只做：

```text
保存当前活动视图状态
→ 绑定目标工作区内存状态
→ 恢复目标视图元数据
→ Hide 旧 HWND / Show 目标 HWND
→ 同步绘制目标 ListView
```

首帧不再发送整窗 `WM_SIZE`、不再无条件调用会逐行回写标签的视图模式设置，也不使用
`RDW_ALLCHILDREN` 同步重绘全部兄弟控件。原生视图模式只做 O(1) 回读；仅在异常漂移时进入
原自修复路径。已验证的 Task Link 状态同样以 O(1) 接管，不再遍历来源分组。分组间距和控件
几何也只在 DPI、布局签名或实际尺寸变化时重设。

有效性判断把“安全呈现”与“最新内容”分开。只有曾由结构完整快照绘制、且工作区 ID/类型、
配置指纹、固定项、原生固定分组、Task Link 上下文、文本查询和全部布局参数均匹配的帧才能
立即呈现。若后台
为同一配置发布了更新 revision，旧完整帧先作为可交互首帧显示，40 ms 后再按当前完整快照
重建；工作区 ID 和切换代数会丢弃快速 A → B → C 中的过期任务。残缺帧和跨配置帧仍禁止
热呈现。详见 `WORKSPACE_CONTENT_INTEGRITY.md`。

旧工作区 worker 的进程终止与 IPC 清理也在目标 ListView 首帧之后执行，避免文件数量不多时
仍被进程/磁盘收尾拖慢。Tab 点击只保留 1 ms 的最新目标合并窗口。热视图还会恢复生成该帧的
内存扫描对象，因此首帧后也不需要同步读取 SQLite/INI；维护任务延到 16 ms，让系统先绘制一帧。

## 缩略图后台生成

Windows Shell 的 `IShellItemImageFactory::GetImage` 即使从 Timer 调用，仍运行在 AHK 的同一个
GUI 线程。视频、PDF、云盘占位符或第三方缩略图处理器可能使一次缓存未命中阻塞数百毫秒，表现
正是 Tab 已经按下但工作区 Change 回调迟迟不能运行。

当前主进程的可见期增强路径完全不调用 `IShellItemImageFactory`。它复用已随包提供、已有超时与
内存隔离的 `PopDropPreview.exe` 协议，建立一套只服务 ListView 的独立共享内存会话。WIC 解码、
文件读取、Shell 缓存查询、缓存未命中生成和 PNG/JPEG 编码均在 Helper 中串行完成；主进程以
30 ms 零等待事件检查取得结果，只复制一张最大 512 px 的已验证预乘 BGRA 位图，并执行一次
`ImageList_Add` 与单行图标替换。

首次请求优先读取现有预览缓存或在 Helper 中直接缩放图片；视频等格式没有现成内容时，再发送
低优先级缓存生成命令，成功后从同一缓存回读像素。该流程没有 AHK BMP 中间文件，也没有修改
扫描 worker 的 INI 协议。普通请求 5 秒、缓存生成 12 秒达到上限会终止 Helper；进程受 512 MiB
Job Object 限制，传输级故障最多重试一次。

应用前会同时核对工作区 ID、ListView HWND、ImageList、队列代数、行号和规范化路径。用户切走
时，完成结果可直接写入仍有效的原工作区隐藏热视图；若该视图已刷新、重建或销毁，结果直接
丢弃。Helper 首次启动还需避开正在提交的 Tab、待提交目标以及最后一次 Tab 输入后的 120 ms，
因此进程创建不会插入点击与目标首帧之间。

内容首帧本身不再调用 `IShellItemImageFactory`，包括 cache-only 查询。每个工作区仍按
“路径 + 修改时间 + 内置图标”保存 ImageList 索引；缓存未命中的普通文件先使用按扩展名共享的
Shell 类型图标，因此同一工作区的一百个 `.txt` 文件只需要创建一份类型图标。`.exe`、`.lnk`、
`.ico` 等文件的特定图标也不在同步首帧读取。真实缩略图随后逐项替换；每个 Timer 只接收和
导入一项，快速切换时仍可随时由 Tab 输入抢占下一项。

## 输入优先的后台提交

扫描 worker 会按来源发布中间结果。旧逻辑在当前已有完整快照时，每收到一个来源就同步清空并
重建整个 ListView；`PopulatePanel()` 又在完整过程使用 `Critical`，因此鼠标已经让原生 Tab 显示
按下状态后，AHK 的 Change 回调仍必须等待这次重建结束。

当前扫描代次拥有独立的 `WorkerPendingScanResult`。中间来源只更新该对象，当前完整扫描对象、
原生 HWND、行路径和 revision 保持不变；worker 完成且结构通过校验后才原子替换。若来源内容
与基线相同则 revision 不变，也不重建列表。需要重建时仅执行一次，并在解除 `Critical` 后交给
Windows 异步绘制。

残缺冷视图的扫描启动等待 180 ms、陈旧热帧更新等待 220 ms；已有完整缓存的自动扫描进一步
等待面板隐藏。切换代数会取消已不属于当前 Tab 的任务。
扫描快照写盘和非活动工作区扫描仅在面板隐藏后运行，避免“首帧之后的延迟任务”阻塞下一次点击。

若实机仍出现超过约 32 ms 的慢切换，程序会把阶段耗时暂存在内存，并在面板隐藏后写入
`workspace-performance.log`。`messageLagMs` 表示 Tab 通知在消息队列中的等待，`dispatchMs`
表示通知到提交的等待，`rememberMs` / `bindMs` / `viewMs` 分别对应旧视图保存、工作区绑定和
视图呈现；`hot=1` 表示真正命中了保留的原生视图。

## 可见期维护隔离

r32 曾尝试改写 worker 请求序列化并让缩略图 worker 跨进程交付 BMP，实机首次显示出现 GUI
线程卡死。当前版本沿用已验证的 r33 扫描协议，并只复用既有原生预览共享内存协议，不再由 AHK
创建或读取 BMP 中间文件。

已有结构完整缓存时，自动扫描、文件监听刷新、精度模式校准和工作区切换校准在面板可见期间
只合并到 `DeferredVisibleScan*` 内存状态。来源键按集合合并，任一全量请求会把最终任务提升为
全量，Recent 标记使用 OR 合并。隐藏后先处理当前工作区的最终任务，再允许非活动工作区扫描。
首次无缓存或结构残缺时仍立即恢复；手动刷新和文件操作完成后的校准也不会延迟。

扫描、持久化和旧 worker 清理仍以隐藏为边界；缩略图不再受此限制，因为其不可信文件和 Shell
工作已全部移入可终止的原生 Helper。窗口保持打开时，真实缩略图会逐项出现，而自动内容更新
仍等待隐藏；需要立即校准文件内容时可使用侧栏刷新按钮。

快速热切换仍只同步交换并异步使目标 ListView 失效。为了修复搜索栏或个别右侧图标偶尔保持
空白有效区的问题，提交尾部会同步完成三个搜索控件和九个 owner-draw 按钮的最终绘制；这些
控件数量固定、图标来自内存 HBITMAP，路径不读取文件、不调用 Shell，也不对 ListView 使用
`RDW_UPDATENOW`。搜索控件在目标 ListView 显示和最终布局之后重新执行原生 `ShowWindow`、
同级 Z 顺序校准和 sibling clipping。工具栏图标预先转换为原生位图；所有自绘子类仅在
`BeginPaint/EndPaint` 成功时吞掉 `WM_PAINT`，失败时回落系统默认过程，避免空白有效区和
持续绘制消息阻塞。工具栏布局仍显式恢复全部按钮和分隔线。

右侧栏采用逻辑 DIP：按钮基准为 32 DIP，操作栏宽度、边缘间距、按钮间距与分隔线使用同一
坐标体系。Windows 负责按当前显示器 DPI 转换，因此 100%/150%/200% 分别得到 32/48/64
物理像素；4K 高缩放下保持原视觉大小，1080p 标准缩放下不会放大为 64 DIP。

后台缩略图传输连续失败三次会打开一分钟熔断，防止 Helper 缺失或被拦截时按文件数反复创建
进程；任一成功响应会自动关闭熔断。映射盘和 UNC 路径允许进入隔离队列，超时仍只终止 Helper，
不会阻塞主窗口。导入成功会只使目标 ListView 行失效，类型图标无需等待整窗重绘即可替换。

活动工作区配置持久化、陈旧扫描 worker 的终止和 IPC 清理也以隐藏为边界。这样可见窗口的
AHK 消息线程只负责输入和已缓存视图交换，不再运行已知可能进入磁盘、进程管理或 Shell 扩展的
维护操作。

首次访问某个工作区仍需创建和填充一次视图；浏览器式速度主要体现在后续往返切换。Recent
侧栏最多只有少量行，当前仍共用一个控件并在切换时重建，不再影响主文件区的大规模行数。

新增的延迟创建视图会注册到现有 OLE DropTarget，继续使用相同的点击、双击、右键、选择、
预览、拖出和拖入入口。窗口尺寸变化时，目标视图从当前活动视图复制几何尺寸。退出时所有
活动与缓存 ImageList 按句柄去重释放。
