# PopDrop v2.0.0

V2.0 继续扩展「顺手」：全新的文本块工作区、一次点按直达目标文件夹的「另存为定位到此」、以及大量细节修复。下面挑重要的说。

## 新功能

### 文本块工作区（v2.0 核心）

- 独立的文本工作区类型，`.md`、`.txt` 文件以卡片形式平铺展示
- 支持多关键字搜索（空格分隔 AND 匹配）、置顶、拖拽归类
- `Enter` 快速发送正文到当前输入位置
- **`Ctrl+Enter` 前置发送**：把文本投送到唤出 PopDrop 前的输入位置
- **Windows Terminal 兼容发送**：识别 Windows Terminal / Console Host，中文正文自动免确认
- 搜索框支持"仅标题"范围、IME 组合输入识别
- 鼠标拖拽卡片即可发送文本；纯键盘也能完成全流程操作

### Tab 切换工作区 & 极速呈现

- 工作区切换从下拉框改为**顶部原生 Tab 栏**，最多显示 8 个，其余进入"更多"菜单
- 右侧新增纯图标工具栏：刷新、粘贴、全部展开/收起、添加/移除固定项、显示、设置、置顶
- 按 F2 唤出面板，**文件瞬间呈现**，不再等待扫描更新
- 双击 F2 直达文本块工作区；F2 首次按下立即显示，不再等待双击判定窗口

### 来源文件夹分栏折叠

- 来源分栏标题单击折叠/展开，收起后不占地方
- 折叠状态按工作区记忆，切换工作区不重置
- 工具栏提供批量展开/收起按钮
- 新增分栏上下间距配置（原生分组边界上增加留白）

### 另存为定位到此

- 从"保存/另存为"对话框唤出 PopDrop 时，每个来源分组标题右侧出现**"另存为定位到此"链接**
- 点击直接导航到对应文件夹，不用再手动翻路径
- 右键菜单保留同样入口
- 只在保存对话框场景显示，普通窗口唤出自动隐藏

### Seer / QuickLook 改进

- **悬停在文件上直接按空格即可预览**，无需先点击选中
- 修复 Seer 空格多次触发、预览窗口落到 PopDrop 下方、A/B 路径串位等问题
- 外部预览期间 PopDrop 自动让出置顶层级

### UI 自定义

- 界面缩放、图标质量、缩略图大小、文件名行数
- 文本块卡片宽高、分栏上下间距
- 设置页新增"界面设置"与"内容更新方式"（极速显示 / 准确优先）

## 修复

- 拖文件经过来源栏时，底部不再误报"已选择 N 个项目"
- F2 隐藏后再打开，固定项栏不会窜成其他工作区的内容
- 跨磁盘移动文件后，旧缩略图立即消失，不留空卡片
- 设置窗口快速操作不再卡死，保存失败自动回滚恢复
- Hover 预览切换工作区不再卡死，可正常恢复
- 文本块→文件工作区切换不再残留顶部空白带
- 空文件夹不显示错缩略图，失效固定项不显示错图标
- 文本块搜索框聚焦时，空格不再误触 Seer 预览

## 系统要求

- Windows 10 或 Windows 11