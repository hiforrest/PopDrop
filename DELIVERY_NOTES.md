# PopDrop v1.1.2 交付说明

## 文本块前置发送

- 文本块工作区新增 `Shift+Enter`“前置发送”；普通 `Enter`、双击和“快速发送”菜单
  仍粘贴到目标当前光标位置，不改变既有行为。
- 前置发送复用现有文本块读取、多选顺序、双换行拼接、使用次数和首尾空行保留规则，不
  新增设置项、发送模式记忆或工具栏按钮；右键菜单提供同名入口和快捷键提示。
- 标准 Edit/RichEdit 通过原生选择消息精确定位并校验开头；浏览器、Electron 等自绘输入
  区使用其通用 `Ctrl+Home` 键盘契约。
- 自动投送前重新验证原窗口、原输入焦点与安全输入框。无法可靠恢复、目标已关闭或位置已
  改变时停止自动粘贴，正文继续保留在剪贴板。

## 原生分栏间距

- “界面设置”的“文件图标与分栏”区域新增分栏上间距、下间距，范围均为 0～32 DIP，
  默认分别为 4、6。
- 配置值是在 Windows 主题原有分组边界上增加的留白，通过 `LVM_SETGROUPMETRICS` 交给
  ListView 原生布局；不拦截滚轮、不关闭重绘，也不覆盖文件区背景。
- 设置保存后立即应用于当前工作区；内存缓存的其他工作区视图在切换时同步应用。

## 侧边栏批量展开、收起与粘贴禁用态

- 新增“全部展开”和“全部收起”图标按钮，只操作当前工作区的 Files、Launcher 与文本来源
  分栏；固定项不参与，切换结果继续按工作区在本次运行中记忆。
- 按参考设计将侧边栏排列为“刷新、粘贴、全部展开、全部收起、添加、移除、显示、设置、
  置顶”，其中“全部展开 / 全部收起”作为独立按钮组，上下均有分割线。
- 批量收起会清除被隐藏项目的选择并关闭预览，批量展开/收起均同步标题符号、传输进度标题
  和 ListView 重绘。
- 粘贴文本块按钮在文件工作区或剪贴板无文本时使用 `btn-paste-gray.png`，同时禁用原生控件、
  鼠标动作、键盘激活、悬停和按下反馈。
- 九个侧边按钮会依据内容区高度统一缩放，正常窗口保持 64 像素，较矮窗口不会发生溢出。

## 缓存生命周期管理

- 新增统一运行时缓存维护器：启动整理、每小时复查、退出完整收尾；15 分钟以上且不属于
  当前任务的扫描 IPC 文件/目录会自动回收，写入中间文件保留 1 小时安全窗口。
- 非当前工作区扫描现在也会在退出时终止并删除请求与结果目录，修复 `inactive-*` 持续累积。
- SQLite 损坏备份默认保留 7 天且最多 3 组；已删除工作区的 INI/SQLite 孤立快照会回收，
  旧 v1～v3 扫描缓存会淘汰。
- WAL 使用 1 MB 尺寸目标和 256 页自动检查点，正常退出执行截断检查点；运行中的 WAL/SHM
  不会被直接删除。
- 设置“内容更新方式”页新增自动管理开关、1～90 天保留期限、占用与最近整理结果、
  “立即整理”和“打开缓存目录”。整理状态写入 `cache-maintenance.ini`。

## 工作区排序

- “管理工作区”列表新增“上移 / 下移”按钮，首项不能继续上移、末项不能继续下移。
- 调整后立即保存工作区顺序并刷新主面板；Tab、“更多”菜单、`Ctrl+数字键` 与
  `Ctrl+Tab` 都按新顺序工作，当前工作区及列表选中项保持不变。

## 来源文件夹分栏折叠

- Files、Launcher 与文本来源标题前显示状态符号：`⋁` 为展开，`⋀` 为收起。
- 单击来源标题即可收起整栏项目，再次单击恢复；固定项和空工作区占位不参与折叠。
- 状态按工作区与来源在当前进程内记忆，刷新、后台内容提交和快速切换工作区不会重置；
  应用重启后默认展开。
- 收起时会取消该栏隐藏项目的选择与预览。右键来源管理、标题拖放命中和下载进度文字保持
  原有行为。
- 标题单击由 `WM_LBUTTONDOWN/WM_LBUTTONUP` 配对并使用原生分组命中测试；按下时即与
  文件选择/单击打开手势隔离，拖出标题后释放不会误切换，也不会选中标题下方文件。

## 设置页与图片预览细节

- PDFium 组件与空格键快速预览配置整体下移，为上方“显示文件名、大小和修改时间”选项
  留出空间；文件信息选项与“后台生成静态快照”复选框左对齐。
- 图片预览中的大小和修改时间使用 14 像素文字；文件名继续使用原来的 15 像素文字。

## 固定项失效路径清理

- 右键“固定项”标题可选择“清除全部失效项目”；菜单在没有失效项时保持禁用。
- 清理范围只包含当前工作区的固定项，不删除任何真实文件，也不改变其他工作区；配置保存
  失败时恢复原列表并显示错误。

## 文本搜索输入按键归属

- 文本块搜索框聚焦期间，Space 固定作为输入键，不再因鼠标悬停文件卡片而启动 Seer 或
  QuickLook。
- IME 组合输入期间，Enter 固定交给输入法完成上屏；组合结束且存在选中结果时，Enter
  继续快速发送文本块。
- IME 判断使用 IMM32 实时查询和开始/结束组合消息兜底；搜索清理和失焦会复位消息状态，
  不会因异常输入法消息长期禁用 Enter。

## Ctrl+Tab 可靠循环切换

- PopDrop 主窗口处于前台时，`Ctrl+Tab` 固定切换到下一个工作区，最后一个之后回到第一个；
  `Ctrl+Shift+Tab` 固定反向切换，第一个之前回到最后一个。
- 两个组合键均强制使用键盘钩子，并以原生前台 HWND 判断作用域，避免按键被焦点控件或
  Windows 接收。快速连续按键仍进入既有合并与串行提交队列，不会并发重建工作区。

## F2 首帧布局稳定化

- 面板显示前先在隐藏状态完成文件区最终布局，避免从文本块工作区恢复时出现“先空白、后
  上移”的可见中间态。

## F2 恢复文件工作区布局修复

- 隐藏面板从文本块工作区切回文件工作区时，窗口显示后立即执行一次原生布局，避免继承
  文本搜索框占用的顶部高度。

## 文件工作区顶部布局修复

- 切换到文件工作区时显式隐藏文本搜索框、清空查询并恢复文件区布局。
- 切换工作区类型时强制同步一次原生布局，避免从文本块工作区返回后顶部出现空白区域。

## 极速双击 F2

- F2 输入采集允许第二次按键中断尚未完成的首次显示回调，并由独立 KeyUp 事件准确解除
  物理按键锁存。
- 第二线程只提交“切换文本工作区”意图，所有界面切换仍在单线程队列执行。

## F2 无闪烁与双击可靠性

- 每次呼出不再无条件重载配置；只有检测到真实外部配置变化时才重建相关运行时状态。
- 双击窗口改为 400 ms，但单击显示不等待；计时起点位于首次显示完成之后，避免冷路径或
  后台线程让第二次按键失效。

## F2 双击时序修复

- 首次 F2 显示后，后台收尾不会在双击窗口内占用消息循环。
- 单击状态保留到第二次按键实际到达，再根据按键间隔决定隐藏或切换文本工作区。
- 隐藏面板时的工作区维护同样延后，避免首次 F2 的切换收尾抢占第二次按键。

## F2 响应优化

- 第一次按下 F2 立即显示上一帧完整面板，不再先等待 240 ms 的双击判定。
- `LoadSettings`、工作区快捷键重建、刷新策略检查、近期栏更新和后台扫描检查均在首帧
  显示之后执行。
- 第二次 F2 仍切换到默认文本块工作区；过期的首次显示收尾会被代数校验丢弃，避免与文本
  工作区切换竞争。

## 本轮快速切换优化

- 鼠标点击顶部 Tab 与快捷键共用最新目标切换队列；连续点击只提交最后一个目标，单击
  仅增加约 4 ms 的合并窗口。
- 热视图切换的同步路径只负责绑定运行时状态、交换已缓存的原生 ListView 和恢复焦点。
  近期栏刷新、监听器协调、脏源判断及后台扫描均在切换返回消息循环后执行。
- 延迟收尾带工作区 ID 与代数校验，旧工作区的回调不能覆盖当前工作区内容。
- 文件工作区之间不重复更新文本搜索框、工具栏按钮和原生布局；跨文件/文本类型切换仍
  会完整更新 UI。

- 主面板顶部使用最多 8 个可见工作区 Tab，超出的工作区进入“更多”菜单；右侧新增按设计
  分组的 PNG 图标工具栏。每个图标都有 Tooltip、浅蓝 Hover 背景、蓝色描边与按下状态，
  置顶按钮会切换图标并保持状态背景。内容列表和滚动条会为工具栏预留独立空间。

- `Ctrl+数字键` 与 `Ctrl+Tab` 的快速连续工作区切换现在合并到最后一个目标，并由单一提交
  队列串行执行；配置事务处于不可中断区间并显式拒绝重入，避免应用自身写入被误判成
  外部修改。真正的编辑器/同步程序并发改写检测仍然保留。

- 空文件夹的“暂无文件”占位项使用 `assets\empty-folder.ico`；失效固定项使用
  `assets\unknown-file.ico`。源码模式从 assets 加载，编译版使用资源 558/559；图标加载
  失败时明确设置 `I_IMAGENONE`，不会误用 ImageList 的第一张缩略图。

本源码包完成分栏折叠、输入可靠性、刷新、更新与内容呈现的专项迭代。应用版本为 1.1.2，配置版本为 28；
`config.ini` 继续作为可编辑配置，运行时文件索引迁移到 Windows WinSQLite3。

## 本次交付

- `Delete` 和右键“删除”直接移入回收站且不确认；`Shift + Delete` 在确认后永久删除，
  两者都支持多选。重命名窗口将文件名与扩展名拆为同一行的独立输入框。
- 设置窗口的“保存”已移到“取消”左侧；“内容更新方式”使用“极速显示（默认推荐）/
  日常首选，极致轻快的使用体验”和“准确优先/保障内容正确，仅限极速模式异常时使用”。
- 文本块搜索新增空格分隔的多关键字 AND 匹配；窗口和搜索结果默认选择第一项，搜索框中
  可直接按 Enter 发送。编辑器保存/另存为会保留现有文件首尾空行。
- `Ctrl+F` 返回搜索框时全选现有查询；关闭并再次呼出文本块工作区时会清空查询、重建
  全量列表，不再显示与空搜索框不一致的旧筛选结果。

- QuickLook/Seer 现在作为完整的外部预览会话参与 temporary 自动隐藏判断；查看器主窗口、
  同进程菜单/对话框及 owner 子窗口获得焦点时，PopDrop 保持在其下方且不会关闭查看器。
- Seer 空格处理已收敛为单一边沿触发入口；锁定悬浮路径并按官方最小间隔合并 5000
  请求，消除随机打开失败和 A/B 串位。Seer 首次显示有两秒有界宽限，在其主窗口再次按
  Space 可通过 5005 可靠关闭；菜单、弹窗和对话框不被拦截。
- QuickLook 的既有键盘与窗口会话行为保持不变。查看器关闭时会重新激活 PopDrop 并恢复
  原列表焦点；QuickLook CLI 的异步窗口创建使用五秒有界握手保护。

- “极速显示”在多工作区共享同一来源时会把非当前工作区的扫描结果同时提交到内存与磁盘
  快照，切换后立即显示最新内容；监听器重建不会遗留阻断后续事件的 debounce 锁存。
- 固定项变化不再清空已有来源画面。扫描任务校验工作区/配置指纹，异常退出或超时会自动
  恢复一次；手动刷新可强制替换卡住的全量任务，最终失败会明确显示“加载失败，请点击刷新”。

- 设置导航新增独立“界面设置”页，并将“打开与文件操作”改名为“文件打开与操作”、
  “显示与过滤”改名为“文件显示与过滤”。界面缩放默认固定为 100%；旧配置中的
  `UiScale=System` 会自动迁移为 `UiScale=100`，不再叠加 Windows 系统缩放。
- 主面板的 `UiScale` 已从单纯字体缩放升级为整体缩放：窗口客户区、控件、文字、图标、
  文本块卡片和布局间距同步变化，并继续叠加 Windows 显示缩放。当前设置窗口与独立对话框
  仍保持原有布局尺寸。
- 缩略图尺寸与间距属于原生 ListView 像素参数，不再重复乘以 Windows DPI；系统显示缩放
  200%、`UiScale=100`、`ThumbnailSize=110` 时仍按 110 像素边长显示。
- 设置窗口新增“内容更新方式”页：默认“极速显示”保留缓存优先体验；“准确优先”会在呼出窗口和切换工作区时重新确认当前工作区内容。

- 已访问工作区使用内存/SQLite 快照立即恢复；同一工作区重复呼出不清空列表、不重建
  ImageList，也不因“显示窗口”本身启动全量扫描。
- 首次扫描按来源发布。扫描完一个文件夹就提交该文件夹的完整结果，来源之间无需等待；
  50～75 ms 内到达的结果自然合并为一次 UI 提交。
- 扫描保持单 worker 顺序 I/O，本地来源排在网络来源之前，避免离线网络盘阻挡本地来源。
- 所有工作区的本地来源共享异步 `ReadDirectoryChangesW` 监听器。事件在 120 ms 内折叠，并只重扫变化来源；
  缓冲区溢出、无效/报错/被关闭句柄会触发该来源校准和监听重建。
- 进程启动、跨日首次呼出、睡眠恢复、配置变化和手动刷新执行条件校准。新增
  `ConsistencyCheckMinutes=60`，默认按分钟检查点在窗口隐藏后校验 Dirty、监听失败或不可监听的来源。
- Recent 与来源结果分离；关闭近期栏时完全跳过。UNC、映射盘、WebDAV 目标不在扫描
  关键路径同步 `FileExist`，离线快捷方式不再拖慢无来源工作区。
- 缩略图首帧只取 Shell 缓存或类型图标，Full 模式随后逐项增强；跨刷新复用 ImageList
  和图标缓存。列表提交后强制完成绘制，再更新独立项目计数。
- 来源更新保留同工作区的多选、焦点与顶部可见位置。自动状态固定为短文案，并对
  180 ms 内完成的更新隐藏“更新中”。
- `index.db` 优先放在 `软件目录\cache`。目录不可写或位于网络盘时回退到
  `%LOCALAPPDATA%\PopDrop\cache`；数据库打开/迁移异常时隔离为 `.corrupt-时间` 后重建，
  WinSQLite3 不可用时回退每工作区 INI 快照。

## 兼容与迁移

- 配置自动升至版本 28，并补写 `ConsistencyCheckMinutes=60`，旧版 `ConsistencyCheckHours`
  自动换算，不改变工作区、来源、固定项
  或任何真实文件。
- 首次没有 SQLite 快照时会尝试读取旧 `scan-cache-v4.ini` 作为种子；失败可安全忽略。
- `PopDropTransfer` 源码版本已升至 1.1.2，协议本身未变。正式发布必须在 Windows 上
  重新构建 x64/x86 Helper，不能沿用源码包中的历史二进制。

## 验证

跨平台验证：

```powershell
python -m unittest discover -s tests -v
```

Windows 交付门禁：

```powershell
AutoHotkey64.exe .\PopDrop.ahk --self-test
powershell -ExecutionPolicy Bypass -File .\native\build.ps1
powershell -ExecutionPolicy Bypass -File .\build.ps1
```

前置发送专项实机检查：

- 标准单行 Edit、多行 Edit 与 RichEdit 中分别预填文字、把光标放在中间，按
  `Shift+Enter` 后确认文本块原样出现在第一个字符之前；
- Chrome、Edge 及至少一个 Electron AI 客户端的普通输入框重复上述检查，并确认
  `Enter` 仍只在当前光标位置发送；
- 搜索框聚焦、卡片区聚焦、单选与多选分别验证；多选顺序、双换行和文本块自身首尾空行
  必须与普通发送一致；
- 目标窗口关闭、原输入控件失效及标准密码框场景不得自动粘贴，正文必须留在剪贴板；
- 中文 IME 组合期间 `Shift+Enter` 不得绕过既有组合输入保护。

输入专项实机检查：

- 微软拼音、搜狗输入法分别输入未上屏拼音并按 Enter，确认只完成上屏、不发送文本块；
- 组合结束后再次按 Enter，确认发送当前选中的文本块；
- 英文键盘布局下确认 Enter 发送行为不变；
- 搜索框有焦点且鼠标分别停在文件卡片、工具栏和空白区时输入 Space，确认都只产生空格；
- 搜索框失焦后确认 Seer 与 QuickLook 的 Space 预览行为保持不变。

分栏折叠专项实机检查：

- 在缩略图、列表和文本卡片视图分别单击来源标题，确认 `⋁/⋀` 与项目可见性同步；
- 折叠后刷新、切换工作区再返回，确认状态保持；重启应用后确认默认展开；
- 折叠含选中项目的分栏，确认选择和预览清除且 Enter 不会打开隐藏项目；
- 在折叠标题上验证右键来源菜单、拖入文件和下载进度文字。

还应在 Windows 10/11 实机覆盖：无来源且 Recent 开/关、离线 UNC 快捷方式、SSD/HDD
多来源、递归大目录、连续复制/重命名/删除、睡眠恢复、快速切换工作区、删除/损坏数据库。

当前工作环境未提供 AutoHotkey、Windows Shell/OLE、PowerShell 或 MSVC，因此这里完成的
自动验证是 Python 静态/契约测试；Windows 运行时自测与 Helper/Ahk2Exe 构建必须在
Windows 构建机执行。构建脚本会在发布前再次执行上述门禁。
