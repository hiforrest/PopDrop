# 工作区内容完整性修复

## 故障原因

扫描 worker 会按来源逐个发布结果。旧逻辑在第一个来源完成时就把 `ScanResultLoaded` 设为真，
并允许该对象进入内存快照、磁盘缓存和热视图。若此时切换 Tab、关闭面板或发生文件操作，
只含部分来源的结果可能被长期误认为完整内容。

热视图最初还以扫描结果对象地址作为内容代次，而 worker 会原地修改同一个对象，因此地址
不变不能证明已经显示的行仍与内存内容一致。这会造成空白视图或缺少来源的视图反复命中，
直到手动刷新。

## 修复原则

- `ScanResultLoaded` 只表示已有可用数据；`CurrentScanComplete` 单独表示全部配置来源形成一个
  结构完整的快照。
- 每次内容发布推进 `CurrentScanRevision`。热视图必须同时匹配工作区、配置指纹、内容代次、
  固定项和显示参数，并且快照完整。
- 冷扫描的部分来源结果只保留在内存中，不写 SQLite/INI，不进入热命中，也不覆盖当前完整
  画面。
- 冷扫描界面为每个尚未完成的来源显示“正在加载…”占位；完整标记和来源结构全部校验通过
  后，才一次性提交最终列表并持久化。
- 磁盘缓存会核对来源数量、顺序、名称、路径和非 Pending 状态。旧版本遗留的残缺缓存会
  自动触发全来源扫描，不会再次迁移或写回。
- 非当前工作区若以残缺缓存为扫描基线，会从 Dirty 子集刷新自动升级为全来源刷新。
- worker 声称完成但结构仍不完整时按失败处理并进入既有重试流程。
- 同一进程内关闭后重开，如果当前结果仍不完整，会立即启动恢复扫描。

## 运行模块

主入口包含 `modules\\ScanCacheIntegrity.inc`。原 `modules\\ScanCache.ahk` 仅保留用于既有静态
契约对照，不参与运行，避免附件镜像恢复旧文件后覆盖完整性修复。

## 验证重点

Windows 实机应覆盖：首次无缓存启动、多来源扫描中快速切换、扫描中关闭并重开、删除文件后
自动刷新、切换到其他 Tab 再返回、进程退出后重启，以及使用旧版本生成的残缺 SQLite/INI
缓存启动。
