# 工作区内容完整性修复

## 故障原因

扫描 worker 会按来源逐个发布结果。旧逻辑在第一个来源完成时就把 `ScanResultLoaded` 设为真，
并允许该对象进入内存快照、磁盘缓存和热视图。若此时切换 Tab、关闭面板或发生文件操作，
只含部分来源的结果可能被长期误认为完整内容。

热视图最初还以扫描结果对象地址作为内容代次，而 worker 会原地修改同一个对象，因此地址
不变不能证明已经显示的行仍与内存内容一致。这会造成空白视图或缺少来源的视图反复命中，
直到手动刷新。

## 修复原则

- `ScanResultLoaded` 只表示已有可用数据；`CurrentScanComplete` 单独表示全部配置来源形成一个
  结构完整的快照。
- 每次内容发布推进 `CurrentScanRevision`。热视图只有在工作区、配置指纹、内容代次、固定项、
  原生固定分组和完整布局签名都匹配时才算“最新命中”。
- 已由完整快照绘制、且除内容代次外所有归属与布局条件仍一致的旧热帧，可以作为可交互首帧
  先显示；它不会被当作最新快照持久化，当前完整 revision 会在带工作区 ID/代数校验的延迟
  任务中重建。残缺帧仍不能走这条路径。
- 冷扫描的部分来源结果只保留在内存中，不写 SQLite/INI，不进入热命中，也不覆盖当前完整
  画面。
- 冷扫描界面为每个尚未完成的来源显示“正在加载…”占位；完整标记和来源结构全部校验通过
  后，才一次性提交最终列表并持久化。
- 磁盘缓存会核对来源数量、顺序、名称、路径和非 Pending 状态。旧版本遗留的残缺缓存会
  自动触发全来源扫描，不会再次迁移或写回。
- 非当前工作区若以残缺缓存为扫描基线，会从 Dirty 子集刷新自动升级为全来源刷新。
- worker 声称完成但结构仍不完整时按失败处理并进入既有重试流程。
- 同一进程内关闭后重开，如果当前结果仍不完整，会立即启动恢复扫描。
- 可见窗口切换到其他工作区时不会关闭活动 worker 的完成轮询；计时器只做 O(1) 身份检查，
  返回原工作区后可继续读取完成标记。重新进入残缺视图时也不再仅凭全局 `WorkerRunning`
  跳过恢复，而由启动器合并同一 worker 或安全替换陈旧 worker。
- 热视图命中会连同生成该帧的完整扫描对象一起恢复；这只复制对象引用，不改变扫描内容，且
  避免随后从磁盘加载另一个 revision。零固定项不再通过第一条来源行的 group id 推断分组，
  但工作区、配置、固定项签名和完整扫描门禁不变。
- 工作区同步切换发生异常时，恢复切换前保存的 ListView HWND 与同行映射；如果后台维护失败，
  已呈现的完整视图继续可用。异常只写入 `workspace-switch-errors.log`，不会把半切换状态暴露给
  后续点击、右键、拖放或预览操作。
- 活动扫描不再原地修改 `CurrentScanResult`。每个 worker 使用独立的
  `WorkerPendingScanResult` 与隐藏项诊断映射，中间来源既不推进当前 revision，也不进入热视图、
  磁盘缓存或当前 ListView。完整标记、工作区、指纹和来源结构全部验证后才一次性发布。
- 已有完整基线且新扫描的来源内容完全相同时，保持原 revision 和原生 ListView；Recent 单独变化
  只更新 Recent 侧栏。文件操作待恢复状态仍可强制一次完整提交，保持选择与滚动位置契约。
- 来源和 Recent 的 Dirty 标记只在整个扫描代次成功提交后清除；清除仍核对扫描开始时捕获的
  token，因此扫描期间到达的新文件变化不会被旧结果误清除。
- 非活动扫描队列项在短 `Critical` 区间内从全局 Map 原子认领。认领完成后到达的 Dirty token
  会进入一个新的 Map 项，旧 worker 不会误删；队列合并、前台扫描清理和失败重新入队使用同一
  组原子辅助函数，避免缺失键的二次 `Delete` 弹窗。worker 建立中途失败还会终止已启动进程、
  恢复 Recent 待处理状态并清理请求目录。
- 扫描快照在面板可见期间只进入内存延迟队列，隐藏、设置完整重载或程序退出时再写入 SQLite/INI；
  延迟项按工作区 ID 合并，切换不会把旧工作区结果写入当前命名空间。
- 当前已有结构完整快照时，面板可见期间新到达的自动扫描请求也只进入
  `DeferredVisibleScan*` 合并队列；全量请求优先于来源子集，Recent 请求不会丢失。隐藏后才按
  当前工作区身份启动 worker。首次无缓存、结构残缺、手动刷新和文件操作恢复继续绕过该门禁，
  保证完整性修复和显式用户操作不会被无限延迟。

## 运行模块

主入口包含 `modules\\ScanCacheIntegrity.inc`。原 `modules\\ScanCache.ahk` 仅保留用于既有静态
契约对照，不参与运行，避免附件镜像恢复旧文件后覆盖完整性修复。

## 验证重点

Windows 实机应覆盖：首次无缓存启动、多来源扫描中快速切换、扫描中关闭并重开、删除文件后
自动刷新、切换到其他 Tab 再返回、进程退出后重启，以及使用旧版本生成的残缺 SQLite/INI
缓存启动。
