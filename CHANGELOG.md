# 更新日志

## v1.1.2：文本块前置发送 — 2026-08-09

- 文本块工作区新增 `Shift+Enter`“前置发送”，把当前单选或多选正文投送到呼出 PopDrop
  前的输入内容开头；`Enter`、双击及原“快速发送”继续粘贴到当前光标位置。
- 前置发送沿用多选顺序、双换行拼接与首尾空行保留，不自动添加分隔符，不记忆模式，也不
  增加配置项或工具栏按钮；右键菜单新增可发现入口。
- 标准 Edit/RichEdit 使用可校验的原生开头定位，浏览器与 Electron 输入区使用
  `Ctrl+Home`。原窗口、原焦点或安全性检查不通过时停止自动投送，正文保留在剪贴板。

## v1.1.2：来源文件夹分栏折叠 — 2026-08-08

- “界面设置”新增分栏上、下间距，默认 4/6 DIP、范围 0～32；在系统主题原有分组边界
  基础上增加留白，并通过原生 `LVM_SETGROUPMETRICS` 完成布局和滚动；配置版本升至 28。
- 侧边工具栏新增“全部展开”和“全部收起”：只批量改变当前工作区的来源分栏，固定项保持
  展开；操作同步更新 `⋁` / `⋀` 标题、选择、预览和当前会话的工作区折叠记忆。
- 粘贴文本块按钮在非文本块工作区或剪贴板没有文本时进入真正的禁用状态，使用新增灰色
  图标，不再显示悬停、按下和键盘焦点反馈，也不会响应鼠标或键盘激活。
- 侧边工具栏扩展为 9 个按钮；空间足够时保持 64 像素，窗口较矮时按可用高度统一缩小，
  避免新增按钮越出内容区域。
- 侧边工具栏按钮顺序按设计稿调整为“刷新、粘贴、全部展开、全部收起、添加、移除、显示、
  设置、置顶”；展开与收起组成独立分组，并与相邻按钮用分割线区隔。
- 补齐 `cache` 目录生命周期管理：启动时立即整理，运行期间每小时复查，退出时同时终止并
  回收当前扫描与非当前工作区扫描。崩溃遗留的 `request-*`、`ready-*`、`inactive-*` 在
  超过 15 分钟安全窗口后自动删除，写入中间文件超过 1 小时后回收。
- 损坏的 SQLite 隔离备份默认保留 7 天且最多 3 组；已删除工作区的孤立 INI 快照按保留期
  清理，旧 `scan-cache-v1`～`v3` 自动淘汰，SQLite 中已不存在的工作区记录同步删除。
- 设置“内容更新方式”页新增缓存管理区域，可开关自动整理、设置 1～90 天保留期限、查看
  当前占用和最近整理结果，并可“立即整理”或打开缓存目录。每次整理写入
  `cache-maintenance.ini`，明确记录时间、清理数量和剩余占用。
- SQLite WAL 设置 1 MB 日志尺寸目标、256 页自动检查点，正常退出执行截断检查点；运行中
  的 `index.db-wal` / `index.db-shm` 仍作为正常事务文件保留，不进行危险的直接删除。
- “管理工作区”窗口新增“上移 / 下移”排序；保存后主面板 Tab、“更多”菜单、数字快捷键和
  `Ctrl+Tab` 均使用新顺序。移动后保持原工作区选中，首项与末项的越界按钮自动禁用。
- Files、Launcher 和文本来源分栏标题支持单击折叠/展开；展开状态显示 `⋁`，收起状态
  显示 `⋀`，收起后由原生 ListView 隐藏整栏项目，只保留标题。
- 折叠状态按工作区与稳定来源 ID 在本次运行中隔离记忆，刷新、后台扫描、热视图切换和
  视图重建都会恢复；重启应用后默认展开。
- 收起分栏时清除其中不可见项目的选择和预览，避免 Enter 操作隐藏项目；右键来源菜单、
  分栏拖放命中和下载进度标题继续工作。
- 左键来源标题原有“打开文件夹”行为改为折叠切换；打开来源文件夹仍可从右键菜单执行。
- 修复首版错误依赖不存在的分组标题通知、导致单击标题仍选择文件的问题；现改由鼠标按下与
  抬起阶段的原生分组命中测试完成切换，并拦截标题手势进入文件选择逻辑。
- 设置页将 PDFium 与空格键快速预览两行整体下移，为“显示文件名、大小和修改时间”选项
  留出独立空间；该选项与上方“后台生成静态快照”左对齐。图片预览的大小与修改时间改为
  14 像素，文件名字号保持 15 像素。
- “固定项”分组标题新增右键菜单“清除全部失效项目”；仅清理当前工作区中不存在或当前
  无法访问的固定路径，正常项目与其他工作区固定项不受影响，保存失败时自动恢复。

## v1.1.1：文本搜索输入按键归属修复 — 2026-08-08

- 文本块搜索框获得焦点时，Space 始终保留给文本及输入法输入；即使鼠标仍停在文件卡片上，
  也不会误触发 Seer 或 QuickLook。
- 搜索框内存在中文、日文等 IME 组合输入时，Enter 不再快速发送文本块，而是交给输入法
  完成上屏；组合结束后 Enter 继续保持原有快速发送行为。
- IME 状态同时使用 IMM32 实时组合字符串和 `WM_IME_STARTCOMPOSITION` /
  `WM_IME_ENDCOMPOSITION` 消息判断，并在搜索清理或控件失焦时复位。
- 按键归属判断保留在 `HotIf` 条件层，不在热键回调中二次吞键。

## Ctrl+Tab 可靠循环切换 — 2026-08-08

- `Ctrl+Tab` 与 `Ctrl+Shift+Tab` 改为强制键盘钩子捕获；只要 PopDrop 主窗口位于前台，
  按键就不会再落入原生 Tab、文本框或系统窗口切换路径。
- 快捷键生效条件改为直接核对主窗口 HWND 的可见与前台状态，不再依赖可能在显示/激活
  过渡期间短暂滞后的 `PanelVisible` 状态。
- 正向和反向切换继续通过统一的串行工作区队列执行，并用取模计算实现末项回到首项、
  首项反向回到末项。

## F2 首帧布局稳定化 — 2026-08-08

- 将文件/文本工作区切换后的最终几何布局提前到 `Panel.Show()` 之前计算并应用。
- 修复从文本块工作区关闭后再次 F2 打开文件工作区时，用户先看到旧搜索带高度、随后文件区
  上移的可见过渡。

## F2 从文本块恢复文件区布局 — 2026-08-08

- 修复上次关闭停留在文本块工作区后，F2 恢复文件工作区时继承文本搜索区域垂直偏移，导致
  Tab 下方出现空白带的问题。
- 面板 HWND 变为可见后立即提交一次原生布局；鼠标 Tab 路径和文件工作区之间的快速切换
  行为保持不变。

## 文件工作区顶部布局一致性 — 2026-08-08

- 工作区切换完成后增加搜索框可见性归一化：文件工作区强制隐藏文本搜索框并清空残留查询，
  文本块工作区强制恢复搜索框。
- 文件/文本类型变化或搜索框状态变化时强制提交一次原生布局，避免文本工作区的顶部高度
  偶尔残留到文件工作区。

## 极速双击 F2 输入采集 — 2026-08-08

- 主快捷键启用两个并发输入线程；第二次 F2 可以在第一次 F2 的窗口呈现回调尚未返回时
  进入轻量手势采集器，不再被 AutoHotkey 默认的单热键线程上限丢弃。
- 新增独立的物理 F2 KeyUp 热键，按键释放后立即解除长按锁存；长按自动重复仍被过滤，
  真正的极速第二次按键可以被识别。
- UI 操作仍通过原有单向提交队列串行执行；第二线程只记录第二次按键并请求文本工作区，
  不会并发修改 ListView 或工作区运行时状态。

## F2 无闪烁与双击可靠性 — 2026-08-08

- 面板呼出收尾先比较配置文件时间/大小指纹；配置没有外部变化时不再执行 `LoadSettings`，
  避免固定延迟后重建 Tab、工具栏和文件列表造成的整窗闪烁。
- 应用自身的原子配置写入和完整配置加载都会同步更新指纹，正常状态保存不会被误判为外部
  修改。
- F2 双击窗口从 240 ms 调整为 400 ms，单击首帧仍立即显示；双击计时从第一次显示动作
  完成后开始，冷路径期间排队的第二次按键也能稳定识别。

## F2 双击时序修复 — 2026-08-08

- 修复首帧立即显示后，后台收尾任务可能在第二次 F2 到达前阻塞消息循环，导致双击被
  识别为“显示后隐藏”的问题。
- 单击首次显示期间不再使用独立的单击提交定时器清空双击状态；双击判定改由第二次按键
  到达时按时间窗口决定。
- 隐藏面板下的工作区维护和首帧收尾延后到双击窗口之后，确保第二次 F2 能优先进入文本
  工作区。

## F2 瞬时呈现与双击保持 — 2026-08-08

- F2 第一次按下不再等待双击判定窗口，立即显示已有完整面板画面。
- 配置重载、工作区热键重建、内容刷新检查和首次布局稳定化改为首帧显示后执行。
- 第二次 F2 仍可在双击窗口内切换到默认文本块工作区；切换时会取消过期的首次显示收尾，
  避免后台收尾抢在文本工作区切换之前执行。

## 连续 Tab 切换响应优化 — 2026-08-07

- 原生 Tab 点击改用统一的最新目标提交器，4 ms 内的连续点击会合并为最终目标，避免
  中间工作区重复执行完整切换。
- 已缓存工作区的主列表先完成内存视图切换；近期栏、来源监听器协调和后台扫描检查改为
  返回消息循环后执行，并用工作区代数丢弃过期收尾任务。
- 文件工作区之间切换时不再重复执行工作区类型控件、布局和工具栏同步；仅在工作区类型
  实际变化时更新这些控件。

## v1.1.0 刷新、监听与稳定呈现 — 2026-08-05

### 主面板 Tab 与右侧图标工具栏 — 2026-08-07

- 主面板工作区选择从下拉框改为顶部原生 Tab，最多显示 8 个；其余工作区进入“更多”
  菜单，从菜单切换后当前工作区仍会成为可见 Tab。原有 `Ctrl+数字键`、`Ctrl+Tab` 和
  独立工作区快捷键继续共用串行切换入口。
- 刷新、粘贴文本、添加/移除固定项、显示、设置和置顶移至右侧纯图标工具栏，并按设计稿
  的五条横向分隔线分组。所有按钮都有延迟 Tooltip、浅蓝 Hover 背景、蓝色描边和按下
  反馈；置顶按钮使用开/关两张图标并持续显示当前状态。
- 右侧工具栏使用独立布局带，内容列表、近期栏和原生滚动条都会避让；工具栏及 PNG 图标
  跟随 PopDrop 主面板缩放。退出时释放 GDI+ 图片、子类回调和提示状态。

### 快速工作区切换并发修复 — 2026-08-07

- 修复快速连续按 `Ctrl+1`～`Ctrl+9` 或 `Ctrl+Tab` 时，后一次切换先成功写入配置、前一次
  切换却把该内部写入误报为“其他程序修改配置”的竞态。配置事务现在禁止同进程重入，
  同时继续检测并保护真正的外部修改。
- 内置工作区切换快捷键会在 25 ms 内合并连续请求，并以最后一次目标为准；切换尚未完成
  时的新请求会排队到当前切换之后，不再嵌套刷新界面或重复显示失败对话框。

### 空文件夹与失效固定项图标 — 2026-08-07

- 空来源文件夹的“暂无文件”占位项现在固定使用 `empty-folder.ico`，不再因原生 ListView
  的默认图像索引而误用当前列表中的第一张文件缩略图。
- 固定项目标不存在时固定使用 `unknown-file.ico`，不再根据已经失效的扩展名请求 Shell
  类型图标。两个图标均支持源码运行，并作为资源嵌入编译版。

### 文件操作与设置界面调整 — 2026-08-06

- `Delete` 与 PopDrop 右键菜单“删除”现在不经二次确认直接移入回收站；新增
  `Shift + Delete`，在明确警告并确认后永久删除单个或多个所选项目。两条路径均由
  Windows Shell 执行，回收站删除失败时不会回退为永久删除。
- 重命名对话框把文件名和扩展名拆分为同一行的两个文本框；文件夹只显示名称框，文件的
  最后一个扩展名会自动保留，也可单独修改或清空。
- 设置窗口底部调整为“保存”在左、“取消”在右；“内容更新方式”改用更简洁的推荐与
  故障恢复文案。

### 主面板整体缩放 — 2026-08-06

- “界面缩放”现在会同步放大主面板窗口、工具栏控件、列表与状态栏布局、文字、缩略图、
  文本块卡片、角标及各处间距，不再只是增大字体。应用比例仍与 Windows 显示缩放叠加，
  保存后重启生效；本次范围不包含设置窗口及独立对话框的整体几何尺寸。
- 修复 Windows 显示缩放为 200% 时缩略图尺寸被重复乘以 DPI 的问题。`ThumbnailSize`
  继续表示原生像素边长，例如设置为 110 时主面板缩放 100% 就按 110 像素请求和排布；
  仅 PopDrop 主面板缩放会在此基础上按比例调整。

### 文本块搜索、默认选择与空行保留 — 2026-08-06

- 文本块搜索支持使用半角或全角空格分隔多个关键字，按 AND 关系同时匹配文件名、正文、
  来源名和路径；关键字可以分别命中不同字段。
- 文本块工作区在无搜索时默认选中第一个文本块，每次修改搜索词后默认选中第一个结果；
  搜索框内即使查询为空，也可直接按 `Enter` 快速发送默认选择。
- 内置编辑器保存和另存为不再裁掉现有文本首尾的空格或空行。读取、复制和快速发送继续
  原样保留文件边界，避免粘贴后与目标位置相邻文本连在一起。
- `Ctrl+F`（以及 `/`）回到搜索框时会选中全部已有搜索文字，可直接覆盖输入；修复关闭
  文本块工作区后再次按 F2 时输入框虽为空、列表却仍停留在旧搜索结果的问题，再次呼出
  现在会恢复显示全部文本块并默认选择第一项。

### QuickLook / Seer 外部预览会话修复 — 2026-08-06

- 修复 Seer 同时收到全局透传热键、上下文热键和列表 WM_KEYDOWN 导致一次按键多次调用、
  大概率打开失败的问题；PopDrop 现在只保留一个面板空格入口，并对长按自动重复做边沿
  去重。Seer 的 5000 请求遵守官方至少 200 ms 的间隔，快速变化只提交最后一个路径。
- 修复悬浮 A 却打开旧选中项 B：发送前锁定本次绝对路径，兼容 4000/4001 握手时也只
  回复该锁定路径；查看器激活后不再执行失焦前排队的选择更新。首次可见前为 Seer 保留
  两秒有界启动宽限，避免 250 ms 健康检查过早判定失败。
- Seer 主预览窗口获得焦点时再次按 Space 会可靠发送 5005 关闭并将焦点还给 PopDrop；
  Seer 菜单、弹出窗口和对话框继续完整持有键盘。QuickLook 的既有交互保持不变。
- 修复 temporary 模式的原生 100 ms 自动隐藏看门狗未识别外部预览会话，导致点击
  QuickLook/Seer 后 PopDrop 与预览器一起关闭的问题。原生看门狗、前台事件钩子、AHK
  看门狗和延迟隐藏现在统一使用同一会话归属判断。
- 外部预览按主窗口、进程及 owner 链识别其菜单、对话框与插件子窗口，并为 QuickLook CLI
  启动/转发保留五秒有界握手期；悬浮项目直接按 Space 时不会再被窗口创建竞态误关。
- QuickLook 获得焦点后键盘完全交还查看器；Seer 仅在无菜单/弹窗的主预览窗口接管
  Space 关闭动作。关闭外部预览后会激活 PopDrop，并恢复打开预览前的列表焦点。

### 极速显示可靠性修复 — 2026-08-06

- 修复多个工作区引用同一文件夹时，非当前工作区的后台扫描只更新 SQLite/INI、却未更新
  内存快照的问题；后台结果现在先同时发布到内存和持久化快照，再按事件代次清除 Dirty，
  切换工作区不会再取回旧内容。
- 修复监听器在 120 ms 事件合并窗口内重建后，刷新锁存位可能永久保持的问题；重建会释放
  锁存并重新投递尚未处理的当前/非当前工作区变更。
- 固定项增删导致扫描指纹变化时保留当前来源画面，同时立即后台协调新指纹，不再退化为
  只显示固定项并长期停在“正在加载”。
- 扫描 worker 现在绑定并校验工作区与配置指纹；过期结果不会提交。异常退出或 120 秒无
  响应会自动重试一次，失败后显示可操作的错误状态；手动“刷新”会强制替换卡住的全量
  worker。工作区切换取消后台任务时也不再同步重启同一任务。

- 设置中新增“内容更新方式”页面，提供“极速显示（默认）”和“准确优先”；准确优先会在呼出窗口及切换工作区时主动确认当前内容。
- 新增独立“界面设置”页，集中配置界面缩放、图标质量与尺寸/间距、文件名行数及文本块卡片宽高；默认缩放为 100%，不再跟随 Windows 系统缩放。

- 扫描 worker 改为来源级原子发布：每个文件夹完成后立即提交，Recent 和最终完成标记
  分离；本地来源优先于网络来源，机械盘不增加随机 I/O 并发。
- 每个工作区持有独立内存快照和 SQLite 事务快照；`index.db` 优先位于便携软件目录，
  不可写或为网络路径时回退 LocalAppData。数据库异常会隔离后重建，WinSQLite3 不可用
  时保留旧 INI 快照回退。
- 本地来源接入异步 `ReadDirectoryChangesW`，支持事件合并、缓冲区溢出、句柄异常、
  配置变化和睡眠恢复后的单来源校准；未受监听覆盖的来源在呼出时后台检查重连。
- 同一工作区重复呼出时不再清空 ListView 或重建 ImageList；来源更新在关闭重绘期间提交，
  恢复选择、焦点和滚动位置，项目数只在对应内容完成绘制后更新。
- 缩略图首帧只使用 Shell 缓存或类型图标，未缓存的 Full 缩略图延后逐项增强；ImageList
  与路径/版本图标缓存跨刷新复用，减少 SSD 上约一秒内的分阶段闪烁。
- 关闭近期栏时完全跳过 Recent。启用时不再同步探测 UNC、映射盘或 WebDAV 目标，避免
  离线快捷方式拖慢没有任何来源的工作区。
- 所有工作区的本地来源共享异步监听器；切换工作区优先使用健康缓存，仅对 Dirty、监听
  失败或不可监听的来源执行定向扫描。新增 `ConsistencyCheckMinutes=60`，只在呼出时判断
  检查点，并在隐藏窗口后执行低优先级一致性校验；旧版小时配置自动兼容。
- 后台状态收敛为“正在加载 / 更新中 / 已更新 / 已是最新 / 更新失败”，180 ms 内完成的
  自动更新不闪“更新中”；配置版本升至 27，应用与 Transfer helper 版本升至 1.1.0。

## v1.0.0-pre02 文本块工作区 — 2026-08-01

### 文本块分类内置顶 — 2026-08-04

- 文本来源卡片新增“在当前文件夹置顶 / 取消置顶”，搜索结果与跨来源多选沿用
  卡片的稳定来源身份，不会把同路径在其他来源中的状态一并修改。
- 每个来源使用独立持久化顺序；置顶项完整显示在普通项之前且不占普通数量配额，普通项
  继续使用既有智能排序。单个置顶项可在同一来源内拖拽调整顺序。
- 置顶状态在卡片右下角显示用户提供的 `assets/pin.ico`，位置与固定文本链接图标一致；
  编译版嵌入资源 557，源码版从资源文件加载并按 DPI 缓存。
- PopDrop 执行的重命名、来源内移动、移出来源和回收站删除会同步维护置顶路径；移除来源
  时同时清理其置顶配置。配置版本升至 26。

### 固定项实体与链接语义统一 — 2026-08-04

- 根据实机截图修正链接图标锚点：文件工作区使用 ListView 的 `LVIR_ICON` 矩形定位到缩略图
  或文件图标右下角，不再落到换行文件名上；文本卡片改用独立 2 DIP 边距，使图标明显向
  右并适度向下贴近卡片右下角。
- 文本来源中的 `.md/.txt` 加入固定项后不再从原来源分组隐藏；固定项显示同一路径的快捷
  入口并在右下角绘制链接图标，来源区域继续显示无图标的实体卡片。
- 文件工作区的固定文件和固定文件夹同样显示链接图标。图标使用新增
  `assets/icon-lnk.ico`，源码模式从资源文件加载，编译版同时嵌入资源 556，并按窗口 DPI
  缓存缩放后的 `HICON`。
- 所有文本卡片统一背景、边框和选中样式，移除“未分类”标题前缀；独立文本块与普通实体
  不再依靠背景色区分。
- 当前工作区的独立文本块拖入文本来源后，只有 Shell 确认移动成功的项目才从固定项移除；
  其他工作区对同一文件的引用仍更新为 Shell 返回的真实新路径。取消、失败或部分失败的
  项目继续保留。
- 固定文本链接普通拖拽继续复制，`Shift` 不再能够移动原文件；独立文本块和链接混选时
  默认拒绝移动，按住 `Ctrl` 可以统一复制。
- 暂时不可用的 `.md/.txt` 固定引用继续显示，激活时报告不可用，用户仍可取消固定。

- 修复先显示文件工作区、再切换到文本块工作区时工具栏残留旧按钮边框并相互覆盖的问题：
  控件显隐和移动全部完成后，会按当前窗口 DPI 同步擦除并重绘工具栏父窗口及所有子控件，
  不再依赖鼠标划过触发局部刷新；直接打开文本工作区和工作区往返切换使用同一布局结果。
- 文本块工具栏新增紧凑“📋+”按钮，可将剪贴板中的非空白文字直接保存为未分类固定文本
  块；剪贴板不含文字、仅含空白、只有文件或图片时按钮自动禁用。
- 文本卡片区、列表空白区或面板容器获得焦点时，`Ctrl+V` 执行相同的固定文本块创建；
  搜索框、重命名框、内置编辑器及其他控件保持 Windows 普通粘贴，不做窗口级无条件拦截。
- 固定项操作组左右新增竖向分隔线：文件工作区组合“＋固定项 / －固定项”，文本工作区
  组合“＋固定项 / 📋+”；同时收窄“显示 ▾”按钮，减少工具栏横向占用。
- 修复从 Windows 资源管理器拖入真实文件夹时，面板顶部不显示“添加为来源”区域：现在以
  `Shell IDList Array` 识别 Explorer 的 Shell 文件系统选择，即使数据对象同时声明异步能力
  或辅助 FileDescriptor 格式，也会在悬停阶段安全读取 `CF_HDROP` 并完成文件夹分类；未知
  异步来源、浏览器下载和真正的虚拟文件仍延迟到正式 Drop 才读取。
- 修复文本工作区接收外部文字或 Shell 文件夹时偶发闪烁/失败：自动隐藏不再把 OLE
  `Drop` 回调前的鼠标释放误判为拖拽结束；原生守卫、AHK 看门狗和延迟隐藏任务共同遵守
  有界的输入拖放事务，保存完成前不会隐藏目标 HWND，取消拖拽后也不会遗留永久暂停状态。
- Directory Opus 地址栏等延迟提供 `CF_HDROP` 的来源，在悬停阶段读取为空或暂时失败时
  不再缓存为最终结果；真正投放时会重新读取一次，文件列表等稳定来源仍复用首次成功读取。
- 固定栏中的未分类草稿采用白色卡片背景，与普通灰色固定引用明确区分；选中时仍统一使用
  `#E5F1FB` 背景和 `#0078D7` 描边，保留清晰的多选反馈。
- 未分类草稿拖到文本来源时默认执行移动归类，不再复制出内容分叉的两个文件；Shell 移动
  成功后固定项原位置自动改写为目标目录的新路径。普通固定引用仍使用复制语义，按住
  Ctrl 可显式复制未分类草稿。
- 文本块工作区每次显示或切换进入时都重新聚焦搜索框，不再继承“置顶”等工具栏按钮的
  历史焦点；筛选后默认选中首条结果且不打断连续输入，Enter 直接发送，Down/Tab 进入
  结果列表继续用方向键选择。
- 新增 `TextBlockCardWidth` 与 `TextBlockCardHeight`，分别控制文本卡片宽高；卡片采用原生
  固定宽高布局，增大可用区域后文字会自动显示更多横向内容和更多行。
- 专项根除 temporary 模式偶发常驻顶部：修复拖放期间旧隐藏检查先停止看门狗、拖放收尾仅
  恢复 HWND 却未恢复看门狗的竞态；内部文本拖放现在属于有效暂停所有者，恢复面板时会
  重建完整自动隐藏生命周期。新增 `EVENT_SYSTEM_FOREGROUND` 系统前台事件钩子作为独立
  失焦信号源，不再把核心隐藏行为仅押在 `WM_ACTIVATE` 和 AHK 定时器上；隐藏的失效对话框
  也不再阻断 `HidePanel()`。
- 修复工作区专属快捷键的键盘自动重复与回调内热键重绑竞态：同一次物理按下现在只执行
  一次，释放后才允许下次触发；每次显示都清空上一窗口会话的暂停计数。temporary 模式下
  真实的外部进程前台状态优先于陈旧暂停对象，确保任何残留状态都不能永久禁止失焦隐藏。
- 新增进程全生命周期的原生 Win32 自动隐藏守卫：它不随面板隐藏而停止，也不依赖
  `PanelVisible`、暂停计数或 AHK 自动隐藏定时器；以真实 HWND 可见性和前台进程为准，
  在外部应用取得前台且鼠标释放后直接 `ShowWindow(SW_HIDE)`，再异步同步应用内部状态。
- 预览背景默认使用完全不透明黑色，并支持 `BackgroundColor=#000000` 与
  `BackgroundOpacity=255` 分别配置颜色和透明度；不透明模式不再隐藏窗口采样桌面。

- 根据实机反馈将主快捷键重构为与面板状态解耦的串行手势状态机：单击 F2 始终进入
  最近使用的文件工作区，240 ms 内的快速双击只收敛到默认文本区，不再执行工作区往返切换或
  隐藏面板；输入回调与工作区加载分离，快速第二次按键不会丢失，并过滤长按键盘重复。
- 单击动作提交时立即关闭当前手势；temporary 自动隐藏检查若撞上内部暂停会持续复查，
  不再因一次竞态永久失效。
- 最终收口主快捷键作用域：工作区单击/双击手势仅在面板隐藏时生效；面板显示期间
  F2 立即关闭，并抑制同一快速按键对中的第二次按键，彻底隔离工作区切换和自动隐藏。
- 专项重构 temporary 自动隐藏：面板可见期间运行独立 100 ms 失焦看门狗，不再只依赖
  单次 `WM_ACTIVATE`；鼠标释放后主动收尾遗留的 CudaText/OLE 拖放会话，并在没有真实
  对话框、菜单、快速预览或拖放所有者时修复陈旧暂停计数，防止文本区永久阻止隐藏。
- 设置页将双击目标明确标为“默认文本区”，并只列出文本块工作区。
- 初始配置新增“文本块”工作区并默认绑定双击 F2；配置版本升至 25，旧版含文本工作区
  且尚未设置双击目标时会一次性采用首个文本工作区。
- 修复文本块右键菜单局部变量遮蔽 `Menu` 类而报错；删除未分类草稿前先撤下卡片并
  停止预览读取，失败时恢复固定状态。
- 文本卡片移除缩略图尺寸的空图标槽，增加横向内间距，略微压缩纵向尺寸，并保留
  圆角描边与卡片间留白；修复默认编辑器菜单回调接收额外事件参数时报错。
- 文本卡片改为完整自绘：左右使用对称的 8 逻辑像素内间距，左右外边距各增加
  2 逻辑像素，描边统一采用随 Windows 主题变化的中性灰色，避免原生 Tile 文字贴住右框。
- 修复新建文本块和另存副本后 Win32 文件句柄未关闭的问题；删除、重命名不再被
  AutoHotkey 自身持续占用，已运行旧版产生的遗留句柄升级并重启一次后即可释放。
- 文本卡片普通背景保持 `#E6E6E6`；框选、Ctrl/Shift 多选时使用 `#E5F1FB`
  背景和 `#0078D7` 描边，并主动刷新自绘行，让框选过程和最终结果都清晰可见。
- 修复在文本块工作区移动鼠标、频繁切换文档预览时偶发的 `0xC0000005`：预览结果先在
  映射访问保护期内复制到私有缓冲区，助手关闭与映射解除延迟到读取完成；GDI 位图按行
  复制可见像素，不再以来源行步长整块写入较小的目标 DIB。
- 消除横向滑过多个文件时的预览闪烁：新增 `PreviousPreviewHoldMs=500`，跨越卡片空隙或
  等待下一项加载时继续显示上一帧；新画面在离屏 DC 中完整构建，仅在最终桌面背景采样时
  短暂隐藏，并以完整画布一次性替换，不再提前释放或展示半成品画布。
- 拖出文本同时提供 `CF_UNICODETEXT` 与 `CF_TEXT`；为 CudaText 增加仅按进程名启用的
  拖出粘贴和选区拖入兼容桥，标准 OLE 目标继续走原路径；有效兼容目标现在显示可接收
  光标，不再保留 CudaText 的禁止状态。
- CudaText 选区改在其内部拖动接管前捕获，并提供有界重试；进入有效目标时临时替换
  系统“禁止”光标为可接收手形，离开目标、松开鼠标或退出程序立即恢复用户光标方案。
- 图片预览的黑色衬底改为在桌面像素上合成 80% 不透明黑色，预览图片保持完全不透明。
- 新增文件/文本块两类工作区，旧工作区无损迁移为文件工作区；支持工作区独立快捷键、
  主快捷键双击直达及键盘快速切换。
- 文本来源递归平铺 `.md/.txt`，以横向文本卡片展示；支持直接键入、中文输入法和跨来源
  文件名/正文筛选，并加入可解释的智能使用排序与新内容保护期。
- 外部 Unicode 文字可拖入具体来源直接保存；拖入固定项会创建应用内“未分类”草稿。
  普通固定文本块仍是引用，未分类草稿移除时经确认进入回收站。
- 双击/Enter 快速发送，Ctrl+C 复制正文，F4 编辑；默认拖出正文，按住 Alt 拖出真实文件。
- OLE 拖放改为按当前目标选择适配器，新增 `CF_UNICODETEXT` 的输入和输出实现；文件工作区
  原有 HDROP、虚拟文件、图片和 URL 优先级保持不变。
- 首版文本块配置版本为 24；本轮快捷键默认与迁移调整后升至 25。构建脚本不再依赖
  开发者机器上的硬编码项目路径。

## v1.0.0 源码模块化与工程整理 — 2026-07-31

- **源码模块化重构**：将原 12,996 行的 `PopDrop.ahk` 按功能拆分为 `modules/` 目录下的
  14 个独立模块；入口文件只保留启动、共享状态、初始化和消息注册，不再承载业务逻辑。
- **修复 `--self-test` 启动顺序**：将 `--self-test` 和 `--scan-worker` 参数分派移到
  所有 `#Include` 之后，确保模块中的全局变量和函数在分派前已定义，消除自测失败。
- **工程目录整理**：将开发报告、测试报告、热修复笔记、本地构建配置等开发期文档统一
  移入 `dev_doc/` 目录（gitignored），根目录仅保留发布相关的核心文档。
- **编译系统修复**：将 Ahk2Exe 的 `*.bin` 解释器存根更新为 v2 版本，恢复 v2 脚本编译
  能力。
- 版本号从 `0.10.0` 升至 `1.0.0`，标记项目进入稳定阶段。

## v0.10.0 主窗口重命名 — 2026-07-30

- PopDrop 简洁右键菜单新增“重命名…”；仅单选有效文件或子文件夹时可用。
- 重命名使用 Windows `IFileOperation`，沿用已有的窗口所有者、权限提升、撤销语义、
  操作结果回调、后台刷新和选择恢复链路。
- 成功后同步所有工作区的固定路径；重命名文件夹时，同时修正指向其后代的固定项。
- 拒绝非法 Windows 文件名、已有同名目标、磁盘/共享根目录，以及会使已配置来源路径
  失效的文件夹重命名。
- 新增跨平台重命名契约测试，并在 `--self-test` 中覆盖名称校验和后代路径映射。

## v0.10.0 源码模块化整理 — 2026-07-30

- 将原 12,996 行的 `PopDrop.ahk` 按配置、应用动作、面板 UI、扫描缓存、项目操作、
  右键菜单、指针输入、文件操作、OLE 投放、Shell 拖出、自测和生命周期拆入
  `modules` 目录；入口文件只保留启动、共享状态、初始化和消息注册。
- 模块化使用 AutoHotkey v2 文本 `#Include`，不改函数签名、调用链和全局状态模型。
- 静态契约测试改为按入口中的模块顺序读取完整主程序，并增加每个模块的括号平衡检查。
- 新增 `CODE_ORGANIZATION.md`，记录模块职责、修改约定、验证命令和后续状态收拢方向。

## v0.10.0 分散问题修复 — 2026-07-30

- 主面板工具栏与底栏的实际占区统一为 42 逻辑像素；同步收紧工具栏上下边距，
  并把释放的空间完整交给中间文件列表。根据两轮实机测量，顶部控件行最终相对初版
  下移 1 逻辑像素，使控件上下间距更均衡。
- 修正底栏两行布局：底栏固定为 42 逻辑像素，左侧文字最多显示两行并垂直居中，
  第三行截断；右侧“↓ 下载”改为与左侧区域顶部对齐。
- 新建配置中的“文档”“下载”“桌面”通过 Windows 已知文件夹 API 解析，遵循用户把
  文件夹移动到其他磁盘后的系统设置；旧版默认占位路径会安全迁移，自定义路径不变。
- QuickLook 主窗口只在确实落到 PopDrop 下方时才调整 Z 序，不再由健康检查反复把
  主窗口推到菜单弹窗之上；工具栏菜单现在可正常显示和操作。
- 动作编辑器移除可由 `{date}` 与 `{time}` 组合得到的 `{datetime}` 插入按钮，
  `{size}` 回到完整的三列网格；命令预览改用普通 JPG/PNG 示例文件。
- 主面板底栏缩窄下载入口，左侧状态区支持两行，长路径获得更多可见空间。
- 来源“显示数量”新增“继承全局 / 自定义数量 / 显示全部”三种模式；继承状态使用
  `MaxFilesPerFolder=Inherit` 持久化，修改全局数量时草稿内的继承来源同步更新。
- 选择 Seer 时 QuickLook 路径框显示“无需配置，保持 Seer 运行即可”，切换预览器时
  仍保留原 QuickLook 路径。

## v0.10.0 PDF 可选组件 / 缓存队列热修 — 2026-07-30

- 设置页新增独立“PDF 预览”开关、组件状态和下载入口；用户明确同意后才异步下载
  当前架构的 PDFium，基础程序包继续不携带 6–7 MB DLL。
- 新增 `native\pdfium-component.ini`。默认写入固定 GitHub 发行包地址并从不可变
  发行元数据取得 SHA-256；也可为 x64/x86 配置自托管 HTTPS DLL、ZIP 或 TGZ 及
  SHA-256。压缩包会安全检查并自动解压。源码模式安装到 `native\bin\<架构>`，
  编译版安装到 Helper 同目录。
- 配置新增 `PdfEnabled=0`，默认不启用 PDF 预览；配置版本升至 22。
- 面板隐藏后不再把当前工作区、固定项和最近项整批加入快照队列。只有本次实际悬浮、
  且显示来源不是有效静态缓存的图片或文档，才会在隐藏 10 秒后低优先级生成。
- 缓存身份继续同时使用文件大小和最后修改时间；文件未变时直接复用，任一项变化后
  只在下次悬浮时按需生成，不在关闭面板时批量刷新。

## v0.10.0 PDFium / 文本重叠热修 — 2026-07-30

- PDF 首屏新增可选 PDFium 主渲染路径，使用非 V8/XFA 的原生 `pdfium.dll`、按需随机
  读取和 64 MiB 实际读取预算；PDFium 不可用时仍保留 Windows 内置渲染及 Shell
  回退。渲染器继续位于可终止 Helper 中，不执行 JavaScript、表单或外部资源。
- 新增显式安装脚本 `native\install-pdfium.ps1`。脚本固定到 PDFium
  `152.0.7961.0` 的不可变发布版本，从发布元数据取得 SHA-256 后校验下载；默认只
  安装当前常用的 x64 DLL，避免把 x86 与 x64 体积无条件叠加。
- Markdown/文本不再依赖 `DrawTextW` 对多行矩形的测量与绘制一致性；Helper 先按
  当前字体和可用宽度显式切成最多三条视觉行，再逐条单行绘制，避免字体/DPI 差异使
  下一段覆盖上一段。
- Renderer 规格版本升至 4，使旧的重叠文本快照与失败 PDF 缓存自动失效。
- QuickLook/Seer 层级、设置窗口悬浮抑制、布局和默认 400 宽度保持上一轮行为。

## v0.10.0 实机反馈修正 — 2026-07-30

- 外部快速预览激活期间持续把 Seer/QuickLook 主窗口维持在 PopDrop 之上，关闭时恢复
  查看器原来的置顶属性，避免 QuickLook 始终被主面板遮挡或 Seer 偶发落到下面。
- 设置窗口打开期间抑制悬浮预览，并在列表鼠标消息进入候选状态前用
  `WindowFromPoint` 确认该坐标没有被其他窗口覆盖，修复空白区域误触下层图标。
- PDF 首次生成不再完全依赖资源管理器的 Shell 缩略图提供器；当系统没有注册 PDF
  缩略图扩展时，隔离 Helper 使用原生随机访问流交给 Windows 自带
  `Windows.Data.Pdf`，显式准备并按目标尺寸渲染第一页，再写入原有共享静态快照
  缓存。仍不捆绑 PDFium 或第三方 PDF 组件。
- 修复 Markdown/文本绘制允许单个逻辑行自动换成多行、却只前进一个固定行高，导致
  后续段落覆盖前一段的问题；现在先测量实际换行高度，最多绘制三行并按真实占用高度
  推进，标题和空行另保留轻量间距。
- 压缩“显示与过滤”页上方共享排除列表，将“显示”和“文件内容预览”整体上移；
  Seer/QuickLook 选择、路径与浏览按钮不再被窗口底部操作栏遮挡。
- 将“恢复推荐值”说明移到按钮右侧，消除重叠；默认预览宽度由 320 调整为 400 DIP。
- 保留用户提供的、已经在本机通过编译的 `PopDropPreview.cpp` 兼容性修改，只在 PDF
  渲染与 Markdown 行高相关位置做增量修改。

## v0.10.0：文件内容预览 — 2026-07-29

- 新增 Markdown、文本/日志/配置、常见代码和 CSV/TSV 的有界语义快照；文本最多
  读取 512 KiB，CSV/TSV 限制 30×12，所有内容均绘制为不可交互位图。
- 新增 PDF 第一页和 DOCX 开头内容预览。PDF 使用 Windows 内置渲染并可复用已注册
  Shell 处理器；DOCX
  优先使用隔离 Helper 中的 IFilter 语义提取并回退真实缩略图，不启动 Office。
- 文档缓存未命中使用 120 ms 防闪烁、5 秒长耗时节点和 12 秒硬期限；错误按资源、
  密码、超时、访问和格式分类，负缓存按原因设置。协议升级至 5。
- 缓存键增加 Renderer ID/版本、DPI 和主题版本；文档与图片继续共享 256 MiB /
  1000 项预算、2 MiB 单项上限和同一 LRU 清理。
- 新增默认关闭的 Seer/QuickLook 空格键集成。Seer 使用官方 `WM_COPYDATA`
  4000/4001/5000/5004/5005 命令；QuickLook 仅接受校验后的桌面/便携版路径。
  外部预览期间只暂停 temporary 自动隐藏，不修改持久置顶设置。
- 配置版本升级至 21；新增 `DocumentEnabled` 与 `[QuickPreview]`，设置保存后立即
  生效。
- 主工具栏移除独立的“缩略图”和“近期栏”动态按钮，合并为固定文字“显示 ▾”。
  原生一级菜单以 Radio 管理缩略图/列表，以 Check 管理文件预览/近期栏；工具栏和
  最小窗口宽度没有增加。
- 视图、文件预览和近期栏统一通过单一 setter 修改。菜单、设置保存和配置重载复用
  同一运行时状态入口；写入失败不会只留下虚假的菜单勾选。关闭文件预览继续走既有
  Generation 失效、请求取消和窗口隐藏流程。
- “显示”菜单按按钮的物理屏幕矩形显示；修复高 DPI 下客户区坐标被再次缩放、导致
  菜单明显偏到按钮左侧的问题。打开期间成对暂停 temporary 自动隐藏并抑制预览，
  Windows 原生菜单负责多显示器和屏幕边缘调整。
- 预览窗口与主面板的 DPI 缩放间距由 8 DIP 调整为 4 DIP。
- 主文件区、固定项和“最近打开”栏新增鼠标悬浮与键盘焦点内容预览；首次、切换、
  离开宽限和键盘延迟分别默认 350、120、140、250 ms，鼠标与键盘采用最后明确输入
  优先，不依赖 `ItemSelect` 建立键盘候选。
- 预览会话使用 Hidden、Armed、Loading、Visible、Suppressed 状态，并同时校验
  Generation、路径、列表实例和面板显示会话；切换候选最多保留旧内容 150 ms，
  同一失败文件默认负缓存 60 秒。
- 点击、多选与预览解耦。真正拖拽、固定项排序、框选、滚动、菜单、刷新、工作区和
  视图切换、窗口移动缩放、面板隐藏及退出会取消请求；交互结束后主动读取静止鼠标
  坐标重新命中，无需先晃动鼠标。
- 新增无焦点 owned tool window，按显示器工作区、DPI 和主面板物理坐标选择侧边，
  并在普通文件切换期间锁定；空间不足时静默不显示。图片等比完整显示、不放大小图，
  透明区域只在图像边界内显示棋盘格。
- 修复 Shell 已有缩略图尺寸明显小于预览目标时直接作为最终结果，导致预览容器和
  图片一同过小的问题；此时图片会继续尝试 WIC 原图缩放解码，失败才回退 Shell 图。
- 根据后续 Windows 实机反馈进一步调整获取顺序为自有缓存、WIC 原图、Shell 缓存；
  Shell 缩略图仅在原图超限、解码器不可用或非图片格式时作为最后回退，并在展示端
  等比放大到可用区域，不再维持系统缓存图的微小像素尺寸。
- 修复面板隐藏十秒时后台扫描、设置窗口或文件传输可能使本轮缓存始终无法启动的
  竞争条件；缓存 Helper 本身已使用 IDLE 优先级并严格串行，因此隐藏计时到达后
  直接开始，不再被长扫描无限阻塞。预览 Helper 协议升级至 3，防止新版脚本继续
  静默使用旧版原生组件。
- 后台缓存生成在 WIC 无法解码 WebP、AVIF 等格式时，可调用对应的 Windows Shell
  缩略图处理器生成较大真实缩略图；仍使用 `THUMBNAILONLY` 禁止普通图标回退，
  即时悬浮路径仍保持 `INCACHEONLY`，不会现场触发 Shell 扩展。
- 修复自动侧边在面板移动或缩放后仍沿用旧方向的问题；交互结束后会按新显示器工作区
  重新选择左右侧，面板贴近右边缘时可自动改到左侧。
- 新增常驻 `PopDropPreview.exe`（x86/x64 构建配置）。AHK 与 Helper 使用会话独占
  共享内存和事件；Helper 同步执行 Shell/WIC 时主界面只做零等待事件轮询。单次请求
  3 秒硬超时后由 Job Object 真正终止并惰性重启，60 秒内超过 3 次则本会话静默禁用。
- 内容严格按自有清晰缓存、WIC 原图首帧缩放解码、
  `SIIGBF_THUMBNAILONLY | SIIGBF_INCACHEONLY` Shell 最后回退获取；不使用
  `SIIGBF_CROPTOSQUARE`，不回退到普通文件图标，也不主动 hydrate 仅在线云占位。
- WIC 优先使用 `IWICBitmapSourceTransform`，否则仅在预计展开内存不超限时使用
  缩放回退；默认限制 64 MiB、65535 边长、160 MP、256 MiB 展开内存和 256 MiB
  Helper 进程内存。
- 面板隐藏 10 秒后开始低优先级、单任务缓存生成，每项间至少让出 200 ms、每次隐藏
  会话最多成功生成 50 项；失败不占成功额度，单轮尝试硬上限为 100。无 Alpha
  使用约 82 质量 JPEG；Alpha 使用压缩真彩 PNG；
  1024/768/512 逐级降级后仍超过 2 MiB 则不落盘。
- 修复缓存 Helper 启动失败或单项超过 3 秒时整条隐藏队列停止的问题；后台失败现在
  只跳过当前项目并继续下一项，也不计入即时预览熔断。新增
  `preview-cache-v1\cache-status.ini`，记录调度阶段、剩余队列及成功/失败数量。
- 修复 WIC 编码提交后尚未释放独占输出流便再次打开临时文件执行
  `FlushFileBuffers`，导致所有缓存统一遭遇共享冲突、随后被当成失败删除的问题；
  现在先释放编码器与流的 COM 链，再重新打开并刷新。Helper 协议升级至 4。
- 缓存键包含稳定文件身份（无法取得时回退规范路径）、大小、最后修改时间、尺寸桶和
  规格版本，文件名使用 SHA-256。清理只允许 `preview-cache-v1` 下已验证名称的普通
  文件，拒绝重解析点，并限制 256 MiB / 1000 项。
- 新增 `[Preview]` 配置节和设置页三个公开选项；配置版本升级至 20，旧配置自动插入
  默认值与注释。更新 README、USAGE、原生构建说明和文件预览测试报告。
- 未引入 pngquant、libimagequant、GPL 或商业依赖；本版本在无法可靠提供内建高质量
  调色板量化时采用真彩 PNG、尺寸递降和 2 MiB 硬上限。

## v0.9.0：工作区 — 2026-07-26

- “在文件管理器中显示”新增 Double Commander、FreeCommander XE 与 Files 支持。
  Double Commander 通过 `-C <完整项目路径>` 定位，FreeCommander XE 通过
  `/C <完整项目路径>` 定位，Files 通过 `-select <完整项目路径>` 定位；三者均支持
  单文件精确选择，多选按父目录去重后打开。配置版本升级至 19，已有文件管理器选择和
  路径保持不变。经官方接口调研，One Commander 只承诺打开路径、没有指定项目选择
  语义，因此未加入 Provider。
- “在文件管理器中显示”新增 XYplorer 支持。设置页可选择 `XYplorer`、自动查找或
  浏览选择 `XYplorer.exe`；单个项目会打开其父目录并精确选中，多选按父目录去重
  后打开。配置版本升级至 18，升级时保留现有文件管理器选择和程序路径，并为缺少
  `[FileManager]` 的旧配置显式补齐 Windows 系统默认值。
- 修复第三方程序参数被无条件加引号、导致 DOpusRT 可能无法识别 `"/acmd"` 和
  `"Go"`、表现为命令已发送但 Directory Opus 无动作的问题；简单控制参数现在保持
  裸形式，只有空值及包含空格、制表符或双引号的参数才执行 Windows 标准引用。
  Directory Opus 命令同时改为
  `NEWTAB=deflister,findexisting TOFRONT`。
- 修复文件项目右键菜单调用“在文件管理器中显示”时，AutoHotkey 菜单事件尾参进入
  `RevealItemsInFileManager`、进而弹出“Too many parameters passed to function”的
  问题；新增精确接收三个菜单事件参数的专用包装器，菜单不再直接绑定业务接口。
- 设置页两项文件管理器测试增加页内进度和结果反馈，打开测试改用系统临时目录；
  Directory Opus 的 `NEWTAB=findexisting,tofront` 在复用现有标签时将 Lister 带到
  前台，避免命令已执行但用户看不到变化。
- 新增统一文件管理器路由及 Windows Shell、Directory Opus、Total Commander、
  XYplorer、Double Commander、FreeCommander XE、Files 七个适配器；所有文件夹打开和文件定位入口共享
  同一语义接口，普通文件打开、Shell
  右键菜单及文件操作保持原样。该路由初次引入时配置版本升级至 17，旧配置缺少
  `[FileManager]` 时继续使用 Windows 系统行为。
- “共享设置 → 打开与文件操作”新增文件管理器选择、程序路径、浏览、常用位置与注册
  信息自动查找、路径检测和两项测试按钮。Directory Opus 使用 `dopusrt.exe /acmd Go`；
  Total Commander 使用 `/O /S /L=` 并在设置页说明暂不支持自动选中文件。
- 修复来源标题右键菜单的局部变量 `menu` 遮蔽 AutoHotkey v2 内置 `Menu`
  类，导致菜单创建时弹出“local variable has not been assigned”错误的问题；
  新增对应静态回归检查。
- 新增主面板来源管理菜单：右键 Files/Launcher 分组标题可打开来源文件夹、后台刷新、
  使用“设置此来源…”精准定位设置，或从当前工作区安全移除；固定项标题、空工作区
  占位和具体文件行保持原有行为。
- 设置精准导航以 WorkspaceId/SourceId 为唯一身份，复用现有设置窗口和
  “保存/放弃/取消”草稿保护；目标来源消失时停留在正确工作区并清空来源选择，不会
  按同名或同路径误选。
- 来源移除使用配置备份和原子事务，只更新目标工作区 `SourceOrder` 并清理 SourceId
  专属节；所有工作区验证通过后才重载界面。活动外部传输按 `TargetSourceId` 阻止
  移除，固定项、最近记录、常用目标、其他工作区及真实文件夹保持不变。
- 新增“拖拽文件夹快速添加为来源”：仅在一次拖拽全部为可验证真实文件夹时，将顶部
  工具栏临时切换为“＋ 添加为来源 / ☆ 加入固定项”双区域，下方 Files、Launcher、
  固定项和最近栏布局及命中保持不变。添加来源只写入当前工作区配置，始终返回 COPY
  或 NONE，不移动、复制或修改真实文件夹。
- 新增 `Unknown / FilesOnly / FoldersOnly / Mixed` 会话分类和单次 HDROP 路径缓存。
  PopDrop 内部拖拽直接使用既有路径；外部对象只有在同步、纯 HDROP 且不含 URL、
  虚拟文件或图片格式信号时才预读一次，Drop 复用缓存。异步 HDROP 继续由 helper
  唯一调用 `GetData(CF_HDROP)`，Chromium 延迟渲染保护不变。
- 快速添加来源复用设置页的来源默认值和序列化字段，通过既有布局感知原子配置事务
  追加 `SourceOrder`；支持当前工作区路径去重、跨工作区同路径、唯一名称、逐项失败
  详情和保存失败零部分写入。新增拖放分类、效果、单次读取、配置保真与回滚自测试。
- 工具动作改用 `PerItem`（逐个项目真正串行）和 `Batch`（一次传入全部）执行模式，
  不再要求用户选择单选或多选；逐个模式通过非阻塞定时检查进程句柄，前一进程退出后
  才启动下一项。
- 动作路径语义统一为 `{folder}`（项目所在文件夹）和 `{parent}`（该文件夹的上一级），
  新增日期、时间、序号、总数和可读文件大小变量；配置版本升级至 16，并兼容迁移旧
  `SelectionMode`、`RequireCommonParent`、`WorkingDirectoryMode=Parent` 及旧
  `{parent}` 参数。
- 动作编辑器的参数区改为左侧多行参数、右侧变量按钮，参数框可显示更多行；命令预览
  增高到至少三行，并能分别预览逐个串行与一次传入全部的命令形态。
- 修复动作编辑窗口向复选框和单选框注册不受支持的 `Change` 事件、导致“添加动作”
  窗口打开时报错的问题；文本框与下拉框使用 `Change`，选择控件使用 `Click`。
- 修复工具动作扩展名校验正则中的 AHK v2 双引号转义错误，避免程序在启动解析阶段退出。
- 新增“应用与工具操作”：同一应用保留一条稳定记录，可独立控制是否显示为打开方式，
  并配置多个带稳定 ID、适用条件、覆盖 EXE、逐项参数和工作目录的工具动作。
- 工具动作支持文件、文件夹、混合选择、两种执行模式、共同文件夹、`<none>` 与
  `.tar.gz` 最长后缀匹配；右键菜单按应用及动作顺序过滤后直接显示前 5 项，其余进入
  “更多工具操作…”，回调固定使用生成菜单时的选择副本和右击项目。
- 动作参数使用 `ArgCount` 与 `ArgNNN` 保存，每行一个参数；`{items}` 展开为多个
  独立参数，其他变量可嵌入单项。执行继续通过 `ShellExecuteExW` 分开传递 EXE、
  参数和工作目录，不经过命令解释器，并关闭返回的进程句柄。
- 配置版本升级至 15；旧应用缺少 `ShowInOpenMenu` 时默认为显示，缺少
  `ActionOrder` 时视为无动作。动作节接入布局感知、未知字段保留、原子写入和删除清理。
- 新增共享设置“默认右键菜单”，可选择 PopDrop 快捷菜单（默认、推荐）或 Windows
  系统菜单；普通右键和菜单键打开默认菜单，`Shift + 右键` 与 `Shift + F10` 统一打开
  另一个菜单，保存后立即生效。
- 移除 `Shift + F10` 的独立热键分派，统一由 ListView 原生 `ContextMenu` 事件处理，
  避免一次键盘操作先后弹出两个菜单。跨父文件夹多选打开系统菜单时只作用于当前项目，
  并通过状态栏给出非阻塞提示。
- `[General] DefaultContextMenu` 接入设置草稿、校验、无损原子写入及失败回滚；旧配置
  缺失、空值或未知值均回退到既有 PopDrop 菜单行为，未知非空值进入配置警告。
- 下载任务中的单个文件一旦安全落盘，主面板立即异步刷新并展示，不再等待后续 5 秒
  图片质量收敛；较大图片筛选、原子替换和重复清理仍在后台执行，批次终态后再刷新一次
  以呈现最终结果。
- 固定项改为按稳定 `WorkspaceId` 独立保存；工作区切换会同步切换固定列表，新建空白
  工作区不含固定项，复制工作区会复制出独立固定列表，删除工作区只移除其配置记录。
- 配置版本升级至 14；旧 `[PinnedFiles]` 按原顺序原子迁移到升级时的当前工作区，
  新配置使用 `[WorkspacePinned:<WorkspaceId>]`，迁移前继续创建 `config.ini.bak`。
- 主面板“工作区：”标签在上一轮基础上再向上移动 2 个物理像素，累计上移 4 像素。
- 修复 Tab3 设置页中的 owner-draw 下拉框未收到父窗口 `WM_DRAWITEM`、导致关闭状态和
  展开列表都没有文字的问题；为实际页面父窗口安装原生窗口子类并转发绘制和测量消息。
- 主面板“工作区：”标签首次单独向上移动 2 个物理像素；托盘菜单“传输中心…”统一改为
  “下载任务…”。
- 修复 owner-draw 下拉框在部分 Windows/DPI 环境中把关闭状态的项目索引报告为
  `-1`、导致设置页所有下拉框文字空白的问题；绘制现在会回退到当前选择索引、
  同步的选项文本副本和控件窗口文本，且不改变控件尺寸或位置。
- 修复文字垂直居中绘制中局部变量 `buffer` 遮蔽内置 `Buffer` 类、导致启动报错的
  问题；同时清理拖放诊断中的同类潜在问题，并新增大小写不敏感构造器遮蔽检查。
- 保持所有控件外框尺寸和位置不变，单独垂直居中下拉框与单行文本框文字；新增
  `UI_DROPDOWN_TEXT_Y_OFFSET_PX` 和 `UI_EDIT_TEXT_Y_OFFSET_PX` 两个物理像素
  微调参数。
- 根据 200% DPI 实机反馈，将下拉框原生文字选择区从 26 调整为 22 个逻辑像素，
  并提供全局物理像素纵向微调参数。
- 新配置默认关闭近期栏，主窗口默认尺寸改为 766×576；已有配置继续沿用保存值。
- “PopDrop 传输中心”更名为“下载任务”，批次按创建顺序倒序显示，最新任务在顶部。
- 设置页右侧内容区顶部增加与左侧导航一致的灰色留白。
- 修复 `FindWorkspace()` 的形参与全局变量仅大小写不同、导致 AutoHotkey v2
  启动时报错的问题；新增全项目“形参与 global 声明冲突”静态回归检查。
- 修复配置有效时 `ValidateConfig()` 返回对象缺少 `Errors` 属性、导致工作区来源
  初始化中断的问题；有效和无效分支现在遵循相同的返回对象契约。
- 修复设置左侧 TreeView 的 `ItemSelect` 回调多声明一个必需参数、点击导航时报错的
  问题；左侧导航现在是唯一可见的页面入口，移除重复的顶部标签栏。
- 收紧“当前工作区”页的底部留白，工作区下拉框改为紧凑宽度并加宽管理按钮；设置页、
  子窗口、传输中心和主工具栏的单行输入、下拉框与按钮统一为紧凑高度且按钮不换行。
- 主面板工具栏为动态文案预留单行宽度；配置中的超宽窗口重新打开时限制为 980，
  避免沿用最大化桌面宽度，窗口显示后仍可自由拖动缩放。
- 主面板新增原生工作区下拉框，切换后自动刷新并只扫描新工作区来源。
- 工作区最初仅保存来源与来源专属设置；本版后续根据反馈将固定项调整为按工作区保存，
  最近文件、软件和常用位置继续共享。
- 设置窗口新增“共享设置 / 工作区设置”左侧导航，以及新建、复制、重命名、删除管理。
- 工作区初版配置版本升级至 13；旧来源原子迁移到“默认工作区”，迁移前创建
  `config.ini.bak`；固定项独立化随后将版本升级至 14。
- 扫描请求、结果和缓存身份加入 `WorkspaceId`，防止切换后的晚到结果串台。
- 设置草稿在切换、管理或关闭前均有保存、放弃和取消路径；保存失败不切换。

## v0.8.8：跨适配器统一图片收敛 — 2026-07-26

- 修复 v0.8.7 的覆盖范围错误：EndOperation 后置观察原先只对 `HDrop` 生效；浏览器
  若通过虚拟文件、PNG/DIB 或 URL 适配器交付被跟踪图片，晚到的浏览器文件不会进入
  清理。
- 目标目录基线现在在选择适配器前建立。所有适配器完成后，从统一任务 `finalPath`
  收集图片计划，并共同执行 EndOperation 后五秒收敛。
- 同批所有最终图片路径都会整体排除，避免多图投放互相删除；只处理相对传输开始基线
  新建或改变、规范文件名匹配、且不属于任何最终任务路径的文件。
- 五秒观察只枚举本批规范文件名对应的候选通配模式，每 200 ms 轮询一次，不反复扫描
  整个下载目录。
- HDROP 的明确网络来源信息继续保留；其他适配器替换时先继承原最终文件的
  Zone Identifier，再合并晚到候选的安全来源信息。
- 修复源码模式 helper 选择顺序：运行 `PopDrop.ahk` 时优先使用刚由
  `native\build.ps1` 生成的 `native\bin\<架构>` 组件；编译发布版仍优先使用程序
  同目录组件。
- 新增严格 helper 版本握手。状态文件报告 `HelperVersion`；缺失或与 PopDrop 版本不
  一致时终止该 helper 并明确提示重新构建，不再悄悄运行残留旧 EXE。
- 静态测试新增基线必须早于适配器选择、统一 `CollectSavedImagePlans()` 和任意图片
  适配器启用收敛检查，以及 helper 路径优先级和版本握手；跨平台测试共 50 项通过。

## v0.8.7：异步结束后的延迟图片收敛 — 2026-07-26

- 根据实机创建时间定位到新的时序根因：helper 保存的大图先完成，来源程序在收到
  `IDataObjectAsyncCapability::EndOperation()` 后约一秒才生成第二张小图。此前所有
  候选检查都发生在 `EndOperation` 前，因此不可能看到这张晚到文件。
- HDROP 图片批次现在在 `EndOperation` 前记录最终文件质量和目标目录快照，调用
  `EndOperation` 后由后台 helper 再执行五秒有界观察。主面板不等待，传输并发槽也
  已释放。
- 只处理相对结束前快照新建或改变、且规范名称属于本批的图片。晚到小图和完全相同的
  副本直接清理；晚到大图通过隐藏 part 和原子替换成为唯一最终文件。
- 后置替换继续使用 WIC 像素面积优先、文件字节数次优的规则，并重新应用网络来源安全
  信息。写入未关闭时等待来源完成；清理或安全标记失败转为 `NeedsAttention`。
- 新增“等大晚到副本”“晚到小图”“EndOperation 必须先于后置收敛”和延迟清理静态
  测试；跨平台测试共 49 项通过。

## v0.8.6：网页图片质量收敛与启动修复 — 2026-07-26

- 修复 `PerformShellFileOperation()` 中 `adoptedPaths` 未在实际函数作用域初始化、启动时
 触发 AutoHotkey `#Warn` 的问题；同时移除误放在回收站函数中的无用初始化。
- 不再无条件优先浏览器已经写入目标文件夹的候选。异步网页图片候选会先等待写句柄
  关闭，再通过 Windows Imaging Component 读取真实宽高：像素面积较大者优先，像素
  相同时再比较文件字节数，最后才以目标内候选作为免复制的平局决策。
- 把 `image.jpg`、`image (1).jpg`、`image (2).jpg` 等同批网页图片安全副本名称归为
  同一候选组，避免 Shell 自动编号掩盖重复关系。
- 补充“未列在 `CF_HDROP` 中的浏览器目标目录副产物”识别：在唯一一次 `GetData`
  前记录目标目录快照，只纳入与本次拖放名称匹配、且相对快照新建或改变的目标图片；
  未变化的既有文件不会被替换。
- 当较大候选位于浏览器缓存、较小候选已经在目标目录时，先写入隐藏
  `.popdrop-part`，刷盘后原子替换本次拖放产生的小图；成功后清理同批其余目标候选，
  最终只保留一份。取消或失败时仍由临时文件析构清理，不留下半成品。
- 新增像素尺寸优先、同尺寸字节数平局、编号副本归组、目标小图被缓存大图取代，以及
  `adoptedPaths` 初始化顺序测试；跨平台测试共 46 项通过。

## v0.8.5：浏览器目标目录文件认领 — 2026-07-26

- 根据实机现象确认新的重复来源：浏览器可能把 `CF_HDROP` 延迟文件直接生成到自己的
  下载目录，而该目录恰好也是 PopDrop 目标。旧 helper 将这个本地路径再次复制回相同
  目录，Shell 安全重命名后形成第二张图片。
- helper 现在使用父目录文件标识比较来源与目标，可识别大小写、规范路径及 junction
  别名。来源已经位于目标目录时，不创建 `.popdrop-part`，不调用
  `OpenDestination()`，也不复制或重命名来源文件。
- 同名候选中只要有一项已经位于目标目录，就优先认领该项，而不是按初始大小选择另一
  个临时路径；避免“小目标文件仍在增长、较大临时候选又被复制回来”的重复。
- 如果浏览器仍持有写句柄，helper 以可取消方式等待其完成；等待期间显示已接收字节但
  不显示虚假百分比。完成后直接认领原路径，并在有网络 URL 时补充来源安全标记。
- 同步外部 HDROP 的 AHK/Shell 链路采用同样的同目录认领规则，并只刷新一次来源。
  普通“复制到…”命令仍允许用户有意在同目录创建副本，不受该规则影响。
- 新增同目录、不同目录、目标子目录、认领先于复制、写句柄等待和外部投放作用域测试；
  跨平台测试共 44 项通过。

## v0.8.4：彻底移除异步 HDROP 主进程预读 — 2026-07-26

- 进一步根据实机“helper 缺失仍先得到一张图片”的结果确认：部分浏览器图片虽然支持
  `IDataObjectAsyncCapability`，却没有通过当前 `QueryGetData` 检测提供明确 URL。
  v0.8.3 因而未进入直接接管分支，而是先读取一次 HDROP、识别为临时路径后再启动
  helper。
- 删除这条“AHK 先 `GetData`、随后 helper 再 `GetData`”的分支。现在所有投向 Files
  来源的外部异步 `CF_HDROP` 都在主进程零提取的情况下直接交给 helper，不再把 URL
  格式作为必要条件。
- helper 缺失检查发生在 marshaling 和任何 HDROP 提取之前；缺失时只显示错误，不应
  再触发浏览器生成第一张未跟踪图片。
- PopDrop 内部拖放以及不声明异步能力的资源管理器、QQ、微信稳定 HDROP 仍读取一次
  本地路径，并继续使用原 `IFileOperation`。
- 静态回归测试明确要求：直接接管策略不得依赖 `HasExplicitUrl`，且源码中不得恢复
  `HDropRequiresAsyncTakeover` 这种读取路径后再决定接管的逻辑。

## v0.8.3：浏览器延迟文件单次接管与回收站删除 — 2026-07-25

- 根据“helper 缺失时报错后仍出现一份文件”的实机证据定位到根因：网页异步
  `CF_HDROP` 曾被主程序和 helper 分别调用一次 `GetData`，Chromium 会把两次读取
  解释为两次独立的延迟文件生成。
- 对同时声明明确 URL 与异步能力的外部 `CF_HDROP`，Drop 现在先检查 helper，再直接
  marshaling 数据对象；AHK 主进程不再预读路径，唯一一次 `GetData(CF_HDROP)` 由
  helper 执行。helper 缺失时不会触发来源文件生成。
- helper 内保留单次返回多候选的网页同名去重；稳定的资源管理器、QQ、微信
  `CF_HDROP → IFileOperation` 链路不变。
- 精简右键菜单在“更多系统操作”上方新增“删除”，并支持 `Delete` 键；操作使用
  `IFileOperation` 回收站语义，没有永久删除回退。
- `ThumbnailPolicy` 默认值由 `Fast` 改为 `Full`；缺少该键时允许 Shell 现场生成
  缩略图，显式配置为 `Fast` 的旧配置仍受尊重。

## v0.8.2：链接图片重复接收修复 — 2026-07-25

- 根据实机反馈补齐 `<a><img></a>` 浏览器拖放的两条实际数据路径，而不再只处理系统
  Temp 下的异步 `CF_HDROP`。
- 当同一数据对象同时提供明确网页 URL 和多个同名 `CF_HDROP` 路径时，在调用原
  `IFileOperation` 前去重；已知大小优先较大项，等大或未知时稳定保留第一项。
- 当网页数据对象提供多个同名 `FILEDESCRIPTORW` 时，在请求 `FILECONTENTS` 之前执行
  同样的选择，因此不会等待并下载第二条网络流。
- 普通资源管理器、QQ、微信和不带网页 URL 的标准虚拟多文件拖放仍保留所有同名项目。
- 新增大小不同、等大、未知大小、非网页来源以及 Shell 操作前去重测试；共 39 项通过。

## v0.8.1：下载入口与浏览器图片修复 — 2026-07-25

- 修复传输中心轮询时删除并重建全部行，导致鼠标选中项立即丢失；现仅在状态确有变化
  时刷新，并按批次 ID 和项目 ID 恢复选择。
- 完成项目固定显示 100%，helper 保留全程平均传输速度，不再在完成时把速度清零。
- 主界面下载入口与普通状态栏合并为同一行并靠右显示。空闲固定为“↓ 下载”；活动超过
  300 ms 后显示整体任务数/进度，活动状态至少保留 800 ms，完成提示保留 3 秒，未查看
  失败持续提示。
- 设置页“外部内容投放”分组改名为“下载”。
- 修复部分浏览器拖拽网页图片时，同时提供同名缩略图和原图临时路径而保存两份的问题：
  仅在异步临时 `CF_HDROP` 链路中合并同名候选并保留较大文件；资源管理器、QQ、微信
  的稳定 `CF_HDROP → IFileOperation` 链路不受影响。
- 跨平台测试增加到 33 项。

## v0.8.0：外部内容投放 — 2026-07-25

- 新增集中格式分类器，固定优先级为 `CF_HDROP` → `FILEDESCRIPTORW/FILECONTENTS`
  → PNG/DIBV5/DIB → 明确 URL；拖动阶段只做 `QueryGetData/EnumFormatEtc`。
- QQ、微信、资源管理器现有 `CF_HDROP → IFileOperation` 链路继续保留；实现异步能力
  且位于临时目录的 HDROP 由 helper 在来源清理前接管。
- 新增独立原生 `PopDropTransfer` 源码、x86/x64 构建脚本和版本化状态协议；COM 数据
  对象使用标准 marshaling，不向另一进程传裸指针。
- 支持多文件 `FILEGROUPDESCRIPTORW`，逐 `lIndex` 读取 `IStream/HGLOBAL`，
  Unicode/空文件/未知大小/部分成功；虚拟目录和 `TYMED_ISTORAGE` 明确拒绝。
- 支持保持原始 PNG，以及通过 WIC 保存 DIB/DIBV5；只有不存在文件流时才进入图片链路。
- 公开 URL 使用 WinHTTP，限制协议/凭据、重定向和超时，识别 Content-Disposition、
  UTF-8 文件名、未知长度、分块和 HTML 登录页；URL 重试凭据使用当前用户 DPAPI 加密。
- 所有后台适配器共用隐藏 `.popdrop-part`、安全自动改名、刷盘、原子落盘和失败清理；
  网络来源通过 `IAttachmentExecute` 标记。
- 主面板新增独立传输状态区、来源标题聚合进度和非模态传输中心；支持取消单项/整批、
  URL 重试、打开目标、批次保留上限和退出保护。
- 新增 28 项跨平台测试、Windows 合成 `IDataObject` 工具、本地慢速 HTTP 夹具和
  独立 Windows 人工验收矩阵。

## 固定项内部排序回归修复 — 2026-07-25

- 修复第一期拖放手势把排序判定限制在“鼠标必须正好命中另一个固定项图块”，
  导致跨过图块留白或列表行非标签区域时提前进入 OLE、无法可靠改变顺序的问题。
- 单个固定项现在在整个固定项原生分组矩形内保持排序态，释放到另一个固定项的
  原生项目边界即可保存顺序；进入 Files/Launcher 分组或离开面板仍切换为 OLE。
- 排序源改用按下行的 `ItemOpenContexts.Area= "Pinned"` 判断，避免同一路径同时
  出现在 Files 来源时被误识别为固定项排序。

## 第一期：本地文件投放 — 2026-07-25

- 新增完整 OLE `IDropTarget`，拖动中持续解析 Files、Launcher、固定项和无效区域，
  同步更新原生分组高亮、状态说明与系统 Copy/Move/禁止光标。
- PopDrop 普通来源之间默认移动，资源管理器、固定项、最近文件和混合选择默认复制；
  `Ctrl` 请求复制，`Shift` 请求移动，危险的 Copy→Move 回退被禁止。
- Files 投放继续复用 `IFileOperation`，支持部分跳过、同来源/同真实路径无操作、
  文件夹自身/后代保护、Shell 实际目标名、固定项路径更新和整批一次刷新。
- Launcher 投放对普通项目创建经验证的唯一 `.lnk`，对已有 `.lnk/.url` 仅复制，
  绝不移动原项目。
- 固定项分组和「＋ 固定项」按钮成为明确投放目标；旧 `DropFiles` 接线已移除，
  同一次投放不会再被重复处理。固定项内部单项排序和向外拖拽继续保留。
- temporary 模式在 OLE 拖入、Shell 操作、取消和异常路径中成对暂停/恢复自动隐藏。
- 增加拖放纯函数自测试、跨平台静态测试和 25 项 Windows 手动测试矩阵。
- 本期仅支持本地 `CF_HDROP`；浏览器 URL、HTTP 下载和虚拟文件不在范围内。

## 0.7.3

- 修复来源类型从“启动器文件夹”切回“普通文件夹”时，启动器显示规则未恢复的问题。
- 普通文件夹现在会恢复按修改时间排序、显示文件名前缀和扩展名，并恢复全局默认的显示范围与文件类型过滤。

## 0.7.2

- “文件来源”页新增“文件夹类型”，可直接选择普通文件夹（`Mode=Files`，默认）
  或启动器文件夹（`Mode=Launcher`）。
- 切换为启动器文件夹时，自动应用仅当前目录文件、名称升序、仅显示
  `.lnk/.url/.exe`、隐藏排序前缀和扩展名等 Launcher 推荐值。
- 来源设置中的“噪音文件”改为“排除噪音文件”；来源附加规则窗口补充
  `*`、`?` 的通俗说明、隐藏效果和示例。
- `[NoiseFilter]` 为每个受管理配置项增加简要注释；升级配置会补充说明，
  且继续保留用户未知配置项和已有注释。

## 0.7.1

- 新增通用临时、锁定及系统文件过滤，默认隐藏 Office/WPS、LibreOffice、Access、AutoCAD 的高置信度占用文件，以及常见目录元数据。
- 新增 Hidden、System、Temporary 属性与未完成下载文件的独立开关；后两项默认关闭。
- 新增文件名通配忽略规则（支持 `*`、`?`），以及每个来源的继承、启用、禁用和附加规则。
- 固定项目始终优先显示；过滤已统一应用于普通/递归扫描、缓存和文件夹最新内容时间计算。
- 新增本次隐藏数量与最多 200 条的只读诊断列表。

## 排除名称弹窗修复 — 2026-07-25

- 将“添加排除名称”从无 Owner 的 `InputBox` 改为设置窗口拥有的模态原生子窗口，
  避免被置顶的设置窗口遮挡。
- 旧配置首次加载全局排除名称时默认加入 `.git`；保存后写入初始化标记，因此用户之后
  主动删除 `.git` 或清空列表仍会被尊重。

## 设置底栏点击修复 — 2026-07-25

- 修复“高级设置…”、“取消”和“保存”使用相对纵坐标后落入 `Tab3` 客户区、
  被标签控件覆盖而无法点击的问题。
- 底栏改用位于 `Tab3` 下方的固定逻辑坐标，继续由 AutoHotkey 自动进行 DPI 缩放。

## 基础设置界面 — 2026-07-24

- 将“文件打开设置”扩展为四页原生 `Tab3`“PopDrop 设置”，使用独立内存草稿，保存前统一校验。
- 新增监控来源添加、移除、改名、改路径、排序和来源详情管理，并继续使用稳定 `SourceId`。
- 新增打开软件、复制/移动常用位置、最近目标清空和全局排除名称的图形化管理。
- 新增来源排除路径和允许覆盖全局排除路径；规则已接入后台扫描 worker，并非仅有界面配置。
- 保存前生成 `config.ini.bak`，继续使用同卷临时文件原子替换；运行时只启动一次必要扫描刷新。
- 配置版本提升为 9；旧配置缺少新字段时保持既有行为。

## 单击释放修复 — 2026-07-24

- 修复部分 Windows ListView 在 `WM_LBUTTONUP` 回调前发送正常
  `WM_CAPTURECHANGED`，导致待处理单击被提前取消、设置为单击后仍无法打开文件。
- 增加原生 ListView `Click` 释放通知作为同一状态机的可靠入口；只有左键仍按住时的
  捕获转移才视为取消，拖拽、框选和捕获丢失保护保持不变。
- 新增捕获释放消息顺序回归测试。

## 单击打开文件增量 — 2026-07-24

- 新增全局 `DoubleClick` / `SingleClick` 文件打开方式，以及每个监控来源的
  `Inherit` / `SingleClick` / `DoubleClick` 覆盖。
- 新增设置窗口、稳定来源 ID 和配置版本 8 迁移；旧配置及损坏值均安全保持双击。
- 单击只在无修饰键、同一文件、未拖拽且未取消的左键释放时触发；文件夹仍双击打开。
- 复用系统拖拽阈值，并保留已选多项的整组拖拽；框选、右键、滚动和焦点/捕获丢失会
  取消本次激活。
- 使用系统双击时间、来源上下文和规范化路径抑制习惯性双击的第二次启动。
- 单击、双击、`Enter` 和右键“打开”统一进入默认关联程序打开函数。

## v0.7.0 修正版 — 2026-07-24

- 简化打开软件配置为 `[OpenApps] Order=id1,id2,...`，删除重复且未生效的单项
  `Order=N` 以及 `App001`、`App002` 键名。
- 新增从 EXE 文件名生成的可读稳定 ID，重名时自动追加数字后缀。
- 兼容并原子迁移旧版 `AppNNN=<UUID>` 配置；旧 UUID 自动转换为可读 ID。
- `Name`、`Icon`、`Enabled` 和空的 `Extensions` 按默认值可省略，降低手工配置成本。
- 再次修复合并旧代码后出现的 AHK v2 大小写不敏感名称遮蔽：
  `menu := Menu()` 与 `pathKey := PathKey(...)` 均改用不冲突的局部变量名。
- 新增构造器/函数自遮蔽回归测试，防止相同问题再次进入交付包。
- 修复自研右键菜单因 GUI 客户区坐标与屏幕坐标混用而远离鼠标显示。
- 修复程序/文件夹选择器可能被置顶的 PopDrop 主窗口遮挡。
- 修复合法 `.exe` 路径被错误截取并拒绝，导致 7-Zip 等程序无法添加和启动。
- 桌面、下载改为 `[TransferFavorites]` 中的可删除配置项，并补齐常用位置写回。

## v0.7.0 — 2026-07-24

- 修复文件操作结果文案中的多行拼接语法，避免启动时报
  `Unexpected "Else"`；新增对应静态回归测试。
- 新增 PopDrop 精简右键菜单；`Shift + 右击`、`Shift + F10` 保留完整 Shell 菜单。
- 新增按扩展名配置 `.exe` 打开程序、自动合并去重、缺失程序修复及撤销入口。
- 新增 `Ctrl + Enter` 多选文件定位、`Ctrl + C` 文件对象复制和
  `Ctrl + Shift + C` 路径文本复制。
- 新增使用 `IFileOperation` 的“复制到…”和“移动到…”，包括常用/最近目标、取消与
  部分完成识别、实际目标路径回调、固定项路径更新和整批一次刷新恢复。
- 固定项菜单支持混合选择状态及可操作数量。
- 新增 `FilesOnly`、`FilesAndFolders`、`RecursiveFiles` 三种显示范围，并兼容迁移
  旧 `IncludeSubfolders`。
- 新增 `DirectoryModified` / `LatestContent` 文件夹排序时间；后台扫描、缓存并跳过
  重解析点。
- v0.7 新配置及固定项批量写入改为临时文件原子替换。
- 新增 Windows `--self-test` 与跨平台静态/逻辑测试。
