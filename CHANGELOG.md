# 更新日志

## v0.9.0：工作区 — 2026-07-26

- 固定项改为按稳定 `WorkspaceId` 独立保存；工作区切换会同步切换固定列表，新建空白
  工作区不含固定项，复制工作区会复制出独立固定列表，删除工作区只移除其配置记录。
- 配置版本升级至 14；旧 `[PinnedFiles]` 按原顺序原子迁移到升级时的当前工作区，
  新配置使用 `[WorkspacePinned:<WorkspaceId>]`，迁移前继续创建 `config.ini.bak`。
- 主面板“工作区：”标签在上一轮基础上再向上移动 2 个物理像素，累计上移 4 像素。
- 修复 Tab3 设置页中的 owner-draw 下拉框未收到父窗口 `WM_DRAWITEM`、导致关闭状态和
  展开列表都没有文字的问题；为实际页面父窗口安装原生窗口子类并转发绘制和测量消息。
- 主面板“工作区：”标签首次单独向上移动 2 个物理像素；托盘菜单“传输中心…”统一改为
  “下载任务…”。
- 修复 owner-draw 下拉框在部分 Windows/DPI 环境中把关闭状态的项目索引报告为
  `-1`、导致设置页所有下拉框文字空白的问题；绘制现在会回退到当前选择索引、
  同步的选项文本副本和控件窗口文本，且不改变控件尺寸或位置。
- 修复文字垂直居中绘制中局部变量 `buffer` 遮蔽内置 `Buffer` 类、导致启动报错的
  问题；同时清理拖放诊断中的同类潜在问题，并新增大小写不敏感构造器遮蔽检查。
- 保持所有控件外框尺寸和位置不变，单独垂直居中下拉框与单行文本框文字；新增
  `UI_DROPDOWN_TEXT_Y_OFFSET_PX` 和 `UI_EDIT_TEXT_Y_OFFSET_PX` 两个物理像素
  微调参数。
- 根据 200% DPI 实机反馈，将下拉框原生文字选择区从 26 调整为 22 个逻辑像素，
  并提供全局物理像素纵向微调参数。
- 新配置默认关闭近期栏，主窗口默认尺寸改为 766×576；已有配置继续沿用保存值。
- “PopDrop 传输中心”更名为“下载任务”，批次按创建顺序倒序显示，最新任务在顶部。
- 设置页右侧内容区顶部增加与左侧导航一致的灰色留白。
- 修复 `FindWorkspace()` 的形参与全局变量仅大小写不同、导致 AutoHotkey v2
  启动时报错的问题；新增全项目“形参与 global 声明冲突”静态回归检查。
- 修复配置有效时 `ValidateConfig()` 返回对象缺少 `Errors` 属性、导致工作区来源
  初始化中断的问题；有效和无效分支现在遵循相同的返回对象契约。
- 修复设置左侧 TreeView 的 `ItemSelect` 回调多声明一个必需参数、点击导航时报错的
  问题；左侧导航现在是唯一可见的页面入口，移除重复的顶部标签栏。
- 收紧“当前工作区”页的底部留白，工作区下拉框改为紧凑宽度并加宽管理按钮；设置页、
  子窗口、传输中心和主工具栏的单行输入、下拉框与按钮统一为紧凑高度且按钮不换行。
- 主面板工具栏为动态文案预留单行宽度；配置中的超宽窗口重新打开时限制为 980，
  避免沿用最大化桌面宽度，窗口显示后仍可自由拖动缩放。
- 主面板新增原生工作区下拉框，切换后自动刷新并只扫描新工作区来源。
- 工作区最初仅保存来源与来源专属设置；本版后续根据反馈将固定项调整为按工作区保存，
  最近文件、软件和常用位置继续共享。
- 设置窗口新增“共享设置 / 工作区设置”左侧导航，以及新建、复制、重命名、删除管理。
- 工作区初版配置版本升级至 13；旧来源原子迁移到“默认工作区”，迁移前创建
  `config.ini.bak`；固定项独立化随后将版本升级至 14。
- 扫描请求、结果和缓存身份加入 `WorkspaceId`，防止切换后的晚到结果串台。
- 设置草稿在切换、管理或关闭前均有保存、放弃和取消路径；保存失败不切换。

## v0.8.8：跨适配器统一图片收敛 — 2026-07-26

- 修复 v0.8.7 的覆盖范围错误：EndOperation 后置观察原先只对 `HDrop` 生效；浏览器
  若通过虚拟文件、PNG/DIB 或 URL 适配器交付被跟踪图片，晚到的浏览器文件不会进入
  清理。
- 目标目录基线现在在选择适配器前建立。所有适配器完成后，从统一任务 `finalPath`
  收集图片计划，并共同执行 EndOperation 后五秒收敛。
- 同批所有最终图片路径都会整体排除，避免多图投放互相删除；只处理相对传输开始基线
  新建或改变、规范文件名匹配、且不属于任何最终任务路径的文件。
- 五秒观察只枚举本批规范文件名对应的候选通配模式，每 200 ms 轮询一次，不反复扫描
  整个下载目录。
- HDROP 的明确网络来源信息继续保留；其他适配器替换时先继承原最终文件的
  Zone Identifier，再合并晚到候选的安全来源信息。
- 修复源码模式 helper 选择顺序：运行 `PopDrop.ahk` 时优先使用刚由
  `native\build.ps1` 生成的 `native\bin\<架构>` 组件；编译发布版仍优先使用程序
  同目录组件。
- 新增严格 helper 版本握手。状态文件报告 `HelperVersion`；缺失或与 PopDrop 版本不
  一致时终止该 helper 并明确提示重新构建，不再悄悄运行残留旧 EXE。
- 静态测试新增基线必须早于适配器选择、统一 `CollectSavedImagePlans()` 和任意图片
  适配器启用收敛检查，以及 helper 路径优先级和版本握手；跨平台测试共 50 项通过。

## v0.8.7：异步结束后的延迟图片收敛 — 2026-07-26

- 根据实机创建时间定位到新的时序根因：helper 保存的大图先完成，来源程序在收到
  `IDataObjectAsyncCapability::EndOperation()` 后约一秒才生成第二张小图。此前所有
  候选检查都发生在 `EndOperation` 前，因此不可能看到这张晚到文件。
- HDROP 图片批次现在在 `EndOperation` 前记录最终文件质量和目标目录快照，调用
  `EndOperation` 后由后台 helper 再执行五秒有界观察。主面板不等待，传输并发槽也
  已释放。
- 只处理相对结束前快照新建或改变、且规范名称属于本批的图片。晚到小图和完全相同的
  副本直接清理；晚到大图通过隐藏 part 和原子替换成为唯一最终文件。
- 后置替换继续使用 WIC 像素面积优先、文件字节数次优的规则，并重新应用网络来源安全
  信息。写入未关闭时等待来源完成；清理或安全标记失败转为 `NeedsAttention`。
- 新增“等大晚到副本”“晚到小图”“EndOperation 必须先于后置收敛”和延迟清理静态
  测试；跨平台测试共 49 项通过。

## v0.8.6：网页图片质量收敛与启动修复 — 2026-07-26

- 修复 `PerformShellFileOperation()` 中 `adoptedPaths` 未在实际函数作用域初始化、启动时
 触发 AutoHotkey `#Warn` 的问题；同时移除误放在回收站函数中的无用初始化。
- 不再无条件优先浏览器已经写入目标文件夹的候选。异步网页图片候选会先等待写句柄
  关闭，再通过 Windows Imaging Component 读取真实宽高：像素面积较大者优先，像素
  相同时再比较文件字节数，最后才以目标内候选作为免复制的平局决策。
- 把 `image.jpg`、`image (1).jpg`、`image (2).jpg` 等同批网页图片安全副本名称归为
  同一候选组，避免 Shell 自动编号掩盖重复关系。
- 补充“未列在 `CF_HDROP` 中的浏览器目标目录副产物”识别：在唯一一次 `GetData`
  前记录目标目录快照，只纳入与本次拖放名称匹配、且相对快照新建或改变的目标图片；
  未变化的既有文件不会被替换。
- 当较大候选位于浏览器缓存、较小候选已经在目标目录时，先写入隐藏
  `.popdrop-part`，刷盘后原子替换本次拖放产生的小图；成功后清理同批其余目标候选，
  最终只保留一份。取消或失败时仍由临时文件析构清理，不留下半成品。
- 新增像素尺寸优先、同尺寸字节数平局、编号副本归组、目标小图被缓存大图取代，以及
  `adoptedPaths` 初始化顺序测试；跨平台测试共 46 项通过。

## v0.8.5：浏览器目标目录文件认领 — 2026-07-26

- 根据实机现象确认新的重复来源：浏览器可能把 `CF_HDROP` 延迟文件直接生成到自己的
  下载目录，而该目录恰好也是 PopDrop 目标。旧 helper 将这个本地路径再次复制回相同
  目录，Shell 安全重命名后形成第二张图片。
- helper 现在使用父目录文件标识比较来源与目标，可识别大小写、规范路径及 junction
  别名。来源已经位于目标目录时，不创建 `.popdrop-part`，不调用
  `OpenDestination()`，也不复制或重命名来源文件。
- 同名候选中只要有一项已经位于目标目录，就优先认领该项，而不是按初始大小选择另一
  个临时路径；避免“小目标文件仍在增长、较大临时候选又被复制回来”的重复。
- 如果浏览器仍持有写句柄，helper 以可取消方式等待其完成；等待期间显示已接收字节但
  不显示虚假百分比。完成后直接认领原路径，并在有网络 URL 时补充来源安全标记。
- 同步外部 HDROP 的 AHK/Shell 链路采用同样的同目录认领规则，并只刷新一次来源。
  普通“复制到…”命令仍允许用户有意在同目录创建副本，不受该规则影响。
- 新增同目录、不同目录、目标子目录、认领先于复制、写句柄等待和外部投放作用域测试；
  跨平台测试共 44 项通过。

## v0.8.4：彻底移除异步 HDROP 主进程预读 — 2026-07-26

- 进一步根据实机“helper 缺失仍先得到一张图片”的结果确认：部分浏览器图片虽然支持
  `IDataObjectAsyncCapability`，却没有通过当前 `QueryGetData` 检测提供明确 URL。
  v0.8.3 因而未进入直接接管分支，而是先读取一次 HDROP、识别为临时路径后再启动
  helper。
- 删除这条“AHK 先 `GetData`、随后 helper 再 `GetData`”的分支。现在所有投向 Files
  来源的外部异步 `CF_HDROP` 都在主进程零提取的情况下直接交给 helper，不再把 URL
  格式作为必要条件。
- helper 缺失检查发生在 marshaling 和任何 HDROP 提取之前；缺失时只显示错误，不应
  再触发浏览器生成第一张未跟踪图片。
- PopDrop 内部拖放以及不声明异步能力的资源管理器、QQ、微信稳定 HDROP 仍读取一次
  本地路径，并继续使用原 `IFileOperation`。
- 静态回归测试明确要求：直接接管策略不得依赖 `HasExplicitUrl`，且源码中不得恢复
  `HDropRequiresAsyncTakeover` 这种读取路径后再决定接管的逻辑。

## v0.8.3：浏览器延迟文件单次接管与回收站删除 — 2026-07-25

- 根据“helper 缺失时报错后仍出现一份文件”的实机证据定位到根因：网页异步
  `CF_HDROP` 曾被主程序和 helper 分别调用一次 `GetData`，Chromium 会把两次读取
  解释为两次独立的延迟文件生成。
- 对同时声明明确 URL 与异步能力的外部 `CF_HDROP`，Drop 现在先检查 helper，再直接
  marshaling 数据对象；AHK 主进程不再预读路径，唯一一次 `GetData(CF_HDROP)` 由
  helper 执行。helper 缺失时不会触发来源文件生成。
- helper 内保留单次返回多候选的网页同名去重；稳定的资源管理器、QQ、微信
  `CF_HDROP → IFileOperation` 链路不变。
- 精简右键菜单在“更多系统操作”上方新增“删除”，并支持 `Delete` 键；操作使用
  `IFileOperation` 回收站语义，没有永久删除回退。
- `ThumbnailPolicy` 默认值由 `Fast` 改为 `Full`；缺少该键时允许 Shell 现场生成
  缩略图，显式配置为 `Fast` 的旧配置仍受尊重。

## v0.8.2：链接图片重复接收修复 — 2026-07-25

- 根据实机反馈补齐 `<a><img></a>` 浏览器拖放的两条实际数据路径，而不再只处理系统
  Temp 下的异步 `CF_HDROP`。
- 当同一数据对象同时提供明确网页 URL 和多个同名 `CF_HDROP` 路径时，在调用原
  `IFileOperation` 前去重；已知大小优先较大项，等大或未知时稳定保留第一项。
- 当网页数据对象提供多个同名 `FILEDESCRIPTORW` 时，在请求 `FILECONTENTS` 之前执行
  同样的选择，因此不会等待并下载第二条网络流。
- 普通资源管理器、QQ、微信和不带网页 URL 的标准虚拟多文件拖放仍保留所有同名项目。
- 新增大小不同、等大、未知大小、非网页来源以及 Shell 操作前去重测试；共 39 项通过。

## v0.8.1：下载入口与浏览器图片修复 — 2026-07-25

- 修复传输中心轮询时删除并重建全部行，导致鼠标选中项立即丢失；现仅在状态确有变化
  时刷新，并按批次 ID 和项目 ID 恢复选择。
- 完成项目固定显示 100%，helper 保留全程平均传输速度，不再在完成时把速度清零。
- 主界面下载入口与普通状态栏合并为同一行并靠右显示。空闲固定为“↓ 下载”；活动超过
  300 ms 后显示整体任务数/进度，活动状态至少保留 800 ms，完成提示保留 3 秒，未查看
  失败持续提示。
- 设置页“外部内容投放”分组改名为“下载”。
- 修复部分浏览器拖拽网页图片时，同时提供同名缩略图和原图临时路径而保存两份的问题：
  仅在异步临时 `CF_HDROP` 链路中合并同名候选并保留较大文件；资源管理器、QQ、微信
  的稳定 `CF_HDROP → IFileOperation` 链路不受影响。
- 跨平台测试增加到 33 项。

## v0.8.0：外部内容投放 — 2026-07-25

- 新增集中格式分类器，固定优先级为 `CF_HDROP` → `FILEDESCRIPTORW/FILECONTENTS`
  → PNG/DIBV5/DIB → 明确 URL；拖动阶段只做 `QueryGetData/EnumFormatEtc`。
- QQ、微信、资源管理器现有 `CF_HDROP → IFileOperation` 链路继续保留；实现异步能力
  且位于临时目录的 HDROP 由 helper 在来源清理前接管。
- 新增独立原生 `PopDropTransfer` 源码、x86/x64 构建脚本和版本化状态协议；COM 数据
  对象使用标准 marshaling，不向另一进程传裸指针。
- 支持多文件 `FILEGROUPDESCRIPTORW`，逐 `lIndex` 读取 `IStream/HGLOBAL`，
  Unicode/空文件/未知大小/部分成功；虚拟目录和 `TYMED_ISTORAGE` 明确拒绝。
- 支持保持原始 PNG，以及通过 WIC 保存 DIB/DIBV5；只有不存在文件流时才进入图片链路。
- 公开 URL 使用 WinHTTP，限制协议/凭据、重定向和超时，识别 Content-Disposition、
  UTF-8 文件名、未知长度、分块和 HTML 登录页；URL 重试凭据使用当前用户 DPAPI 加密。
- 所有后台适配器共用隐藏 `.popdrop-part`、安全自动改名、刷盘、原子落盘和失败清理；
  网络来源通过 `IAttachmentExecute` 标记。
- 主面板新增独立传输状态区、来源标题聚合进度和非模态传输中心；支持取消单项/整批、
  URL 重试、打开目标、批次保留上限和退出保护。
- 新增 28 项跨平台测试、Windows 合成 `IDataObject` 工具、本地慢速 HTTP 夹具和
  独立 Windows 人工验收矩阵。

## 固定项内部排序回归修复 — 2026-07-25

- 修复第一期拖放手势把排序判定限制在“鼠标必须正好命中另一个固定项图块”，
  导致跨过图块留白或列表行非标签区域时提前进入 OLE、无法可靠改变顺序的问题。
- 单个固定项现在在整个固定项原生分组矩形内保持排序态，释放到另一个固定项的
  原生项目边界即可保存顺序；进入 Files/Launcher 分组或离开面板仍切换为 OLE。
- 排序源改用按下行的 `ItemOpenContexts.Area= "Pinned"` 判断，避免同一路径同时
  出现在 Files 来源时被误识别为固定项排序。

## 第一期：本地文件投放 — 2026-07-25

- 新增完整 OLE `IDropTarget`，拖动中持续解析 Files、Launcher、固定项和无效区域，
  同步更新原生分组高亮、状态说明与系统 Copy/Move/禁止光标。
- PopDrop 普通来源之间默认移动，资源管理器、固定项、最近文件和混合选择默认复制；
  `Ctrl` 请求复制，`Shift` 请求移动，危险的 Copy→Move 回退被禁止。
- Files 投放继续复用 `IFileOperation`，支持部分跳过、同来源/同真实路径无操作、
  文件夹自身/后代保护、Shell 实际目标名、固定项路径更新和整批一次刷新。
- Launcher 投放对普通项目创建经验证的唯一 `.lnk`，对已有 `.lnk/.url` 仅复制，
  绝不移动原项目。
- 固定项分组和「＋ 固定项」按钮成为明确投放目标；旧 `DropFiles` 接线已移除，
  同一次投放不会再被重复处理。固定项内部单项排序和向外拖拽继续保留。
- temporary 模式在 OLE 拖入、Shell 操作、取消和异常路径中成对暂停/恢复自动隐藏。
- 增加拖放纯函数自测试、跨平台静态测试和 25 项 Windows 手动测试矩阵。
- 本期仅支持本地 `CF_HDROP`；浏览器 URL、HTTP 下载和虚拟文件不在范围内。

## 0.7.3

- 修复来源类型从“启动器文件夹”切回“普通文件夹”时，启动器显示规则未恢复的问题。
- 普通文件夹现在会恢复按修改时间排序、显示文件名前缀和扩展名，并恢复全局默认的显示范围与文件类型过滤。

## 0.7.2

- “文件来源”页新增“文件夹类型”，可直接选择普通文件夹（`Mode=Files`，默认）
  或启动器文件夹（`Mode=Launcher`）。
- 切换为启动器文件夹时，自动应用仅当前目录文件、名称升序、仅显示
  `.lnk/.url/.exe`、隐藏排序前缀和扩展名等 Launcher 推荐值。
- 来源设置中的“噪音文件”改为“排除噪音文件”；来源附加规则窗口补充
  `*`、`?` 的通俗说明、隐藏效果和示例。
- `[NoiseFilter]` 为每个受管理配置项增加简要注释；升级配置会补充说明，
  且继续保留用户未知配置项和已有注释。

## 0.7.1

- 新增通用临时、锁定及系统文件过滤，默认隐藏 Office/WPS、LibreOffice、Access、AutoCAD 的高置信度占用文件，以及常见目录元数据。
- 新增 Hidden、System、Temporary 属性与未完成下载文件的独立开关；后两项默认关闭。
- 新增文件名通配忽略规则（支持 `*`、`?`），以及每个来源的继承、启用、禁用和附加规则。
- 固定项目始终优先显示；过滤已统一应用于普通/递归扫描、缓存和文件夹最新内容时间计算。
- 新增本次隐藏数量与最多 200 条的只读诊断列表。

## 排除名称弹窗修复 — 2026-07-25

- 将“添加排除名称”从无 Owner 的 `InputBox` 改为设置窗口拥有的模态原生子窗口，
  避免被置顶的设置窗口遮挡。
- 旧配置首次加载全局排除名称时默认加入 `.git`；保存后写入初始化标记，因此用户之后
  主动删除 `.git` 或清空列表仍会被尊重。

## 设置底栏点击修复 — 2026-07-25

- 修复“高级设置…”、“取消”和“保存”使用相对纵坐标后落入 `Tab3` 客户区、
  被标签控件覆盖而无法点击的问题。
- 底栏改用位于 `Tab3` 下方的固定逻辑坐标，继续由 AutoHotkey 自动进行 DPI 缩放。

## 基础设置界面 — 2026-07-24

- 将“文件打开设置”扩展为四页原生 `Tab3`“PopDrop 设置”，使用独立内存草稿，保存前统一校验。
- 新增监控来源添加、移除、改名、改路径、排序和来源详情管理，并继续使用稳定 `SourceId`。
- 新增打开软件、复制/移动常用位置、最近目标清空和全局排除名称的图形化管理。
- 新增来源排除路径和允许覆盖全局排除路径；规则已接入后台扫描 worker，并非仅有界面配置。
- 保存前生成 `config.ini.bak`，继续使用同卷临时文件原子替换；运行时只启动一次必要扫描刷新。
- 配置版本提升为 9；旧配置缺少新字段时保持既有行为。

## 单击释放修复 — 2026-07-24

- 修复部分 Windows ListView 在 `WM_LBUTTONUP` 回调前发送正常
  `WM_CAPTURECHANGED`，导致待处理单击被提前取消、设置为单击后仍无法打开文件。
- 增加原生 ListView `Click` 释放通知作为同一状态机的可靠入口；只有左键仍按住时的
  捕获转移才视为取消，拖拽、框选和捕获丢失保护保持不变。
- 新增捕获释放消息顺序回归测试。

## 单击打开文件增量 — 2026-07-24

- 新增全局 `DoubleClick` / `SingleClick` 文件打开方式，以及每个监控来源的
  `Inherit` / `SingleClick` / `DoubleClick` 覆盖。
- 新增设置窗口、稳定来源 ID 和配置版本 8 迁移；旧配置及损坏值均安全保持双击。
- 单击只在无修饰键、同一文件、未拖拽且未取消的左键释放时触发；文件夹仍双击打开。
- 复用系统拖拽阈值，并保留已选多项的整组拖拽；框选、右键、滚动和焦点/捕获丢失会
  取消本次激活。
- 使用系统双击时间、来源上下文和规范化路径抑制习惯性双击的第二次启动。
- 单击、双击、`Enter` 和右键“打开”统一进入默认关联程序打开函数。

## v0.7.0 修正版 — 2026-07-24

- 简化打开软件配置为 `[OpenApps] Order=id1,id2,...`，删除重复且未生效的单项
  `Order=N` 以及 `App001`、`App002` 键名。
- 新增从 EXE 文件名生成的可读稳定 ID，重名时自动追加数字后缀。
- 兼容并原子迁移旧版 `AppNNN=<UUID>` 配置；旧 UUID 自动转换为可读 ID。
- `Name`、`Icon`、`Enabled` 和空的 `Extensions` 按默认值可省略，降低手工配置成本。
- 再次修复合并旧代码后出现的 AHK v2 大小写不敏感名称遮蔽：
  `menu := Menu()` 与 `pathKey := PathKey(...)` 均改用不冲突的局部变量名。
- 新增构造器/函数自遮蔽回归测试，防止相同问题再次进入交付包。
- 修复自研右键菜单因 GUI 客户区坐标与屏幕坐标混用而远离鼠标显示。
- 修复程序/文件夹选择器可能被置顶的 PopDrop 主窗口遮挡。
- 修复合法 `.exe` 路径被错误截取并拒绝，导致 7-Zip 等程序无法添加和启动。
- 桌面、下载改为 `[TransferFavorites]` 中的可删除配置项，并补齐常用位置写回。

## v0.7.0 — 2026-07-24

- 修复文件操作结果文案中的多行拼接语法，避免启动时报
  `Unexpected "Else"`；新增对应静态回归测试。
- 新增 PopDrop 精简右键菜单；`Shift + 右击`、`Shift + F10` 保留完整 Shell 菜单。
- 新增按扩展名配置 `.exe` 打开程序、自动合并去重、缺失程序修复及撤销入口。
- 新增 `Ctrl + Enter` 多选 Explorer 定位、`Ctrl + C` 文件对象复制和
  `Ctrl + Shift + C` 路径文本复制。
- 新增使用 `IFileOperation` 的“复制到…”和“移动到…”，包括常用/最近目标、取消与
  部分完成识别、实际目标路径回调、固定项路径更新和整批一次刷新恢复。
- 固定项菜单支持混合选择状态及可操作数量。
- 新增 `FilesOnly`、`FilesAndFolders`、`RecursiveFiles` 三种显示范围，并兼容迁移
  旧 `IncludeSubfolders`。
- 新增 `DirectoryModified` / `LatestContent` 文件夹排序时间；后台扫描、缓存并跳过
  重解析点。
- v0.7 新配置及固定项批量写入改为临时文件原子替换。
- 新增 Windows `--self-test` 与跨平台静态/逻辑测试。
